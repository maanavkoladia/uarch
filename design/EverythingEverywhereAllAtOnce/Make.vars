PROJECT_ROOT := $(dir $(abspath $(lastword $(MAKEFILE_LIST))))

CC = vcs

CC_FLAGS = \
    -sverilog   \
    -full64     \
    -debug_acc+all \
    -debug_region+cell+encrypt  \

SIM_TOOL = dve  \

SIM_TOOL_FLAGS  = \
    -full64     \

