the goal is to port my writeback module in my system verilog project into pure 2005 structural verilog.

we will be doing this one module at a time

I will give you an stdcell lib of macros that you will use to port to structural

DO NOT create your own cells, if you are missing some i will implement/provide for you

i will start with the predictor and btb module in the writeback.sv module

you need to port these modules, replace the code in them to be structural, you are only allowed to edit these 5 files

Take extra time to fully understand how the modules functions and interacts with the rest of the system. 

MIO_Q.sv
ST_Q.sv
ST_Q_logic.sv
ST_Q_MIO_logic.sv

When porting to structural make sure you dont use any always block just assigns. no turnarys or logical and bitwise operators. it has to be fully gate implentations. you can only use assigns just for wires. 

Also make sure to understand the writeback common package.

When unrolling struct, DO NOT make one giant wire, create more ports as needed, that needs to be readable by me

ensure that the outputs of the modules that you update DO NOT have system verilog construct in them. 

then in the module that instatiates that modules that you port, ie in this case writeback.sv in the structural folder, make sure to change the
instantation of them to match the new ports.


Plan a way to port writeback and make sure you understand how to unroll structs and properly implent the writeback logic. 

Let me know if you have any questions, dont make assumes and take time to understand how writeback modules work.

create an MD how writeback work, when we port future modules, you can use it as reference


