@core_common_pkg
@core_stage_latches_pkg
@common_pkg
@RegisterReadpkg
@stdCells
@RegFile.sv
@RR_structural


I want to port my RegisterRead stage modules in my system verilog project into pure 2005 structural verilog.

I will give you an stdcell lib of macros that you will use to port to structural

DO NOT create your own cells, if you are missing some i will implement/provide for you

We will start with the regfile module. do not port any other modules outside of the regfile module.

write all your edits to the structural folder in RR.

general advide for the regfile:
- do not treat this like a normal regfile. use individual 64 bit registers for all of the different registers.
- use muxes to drive out the correct register.

keep all the unit names and variable names similar to the original if possible. 

after making the ported regfile module also update the interface for the regfile module in RR_structural.sv. 

Take extra time to fully understand how the modules functions and interacts with the rest of the system. 

When porting to structural make sure you dont use any always block just assigns. no ternarys or logical and bitwise operators. it has to be fully gate implentations. you can only use assigns just for wires. 

Also make sure to understand the packages. write any define.vh files that you need. 

When unrolling struct, DO NOT make one giant wire, create more ports as needed, that needs to be readable by me

ensure that the outputs of the module that you update DO NOT have system verilog construct in them. 

then in the module that instatiates that modules that you port, ie in this case RR_structural.sv in the structural folder, make sure to change the
instantation of them to match the new ports. Note: do not port the RR_structural.sv file itself to structural. only update the regfile interface in the RR_structural file

Plan a way to port the regfile and make sure you understand how to unroll structs and properly implent the regfile logic. 

Let me know if you have any questions, dont make assumes and take time to understand how the regfile works

If you need to read any other files, let me know.