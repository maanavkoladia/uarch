@core_common_pkg
@core_stage_latches_pkg
@common_pkg
@stdCells
@mem.sv
@mem_miss_stall_logic.sv


I want to port my mem stage module in my system verilog project into pure 2005 structural verilog.

I will give you an stdcell lib of macros that you will use to port to structural

DO NOT create your own cells, if you are missing some i will implement/provide for you

you need to port the following modules. do not edit the modules themselves. write the ported modules to the structural folder under the MEM directory

mem_miss_stall_logic.sv
MEM.sv

Take extra time to fully understand how the modules functions and interacts with the rest of the system. 

When porting to structural make sure you dont use any always block just assigns. no ternarys or logical and bitwise operators. it has to be fully gate implentations. you can only use assigns just for wires. 

Also make sure to understand the packages. write any define.vh files that you need. 

When unrolling struct, DO NOT make one giant wire, create more ports as needed, that needs to be readable by me

ensure that the outputs of the modules that you update DO NOT have system verilog construct in them. 

then in the module that instatiates that modules that you port, ie in this case MEM.sv in the structural folder, make sure to change the
instantation of them to match the new ports.

Plan a way to port the mem stage and make sure you understand how to unroll structs and properly implent the fetch logic. 

Let me know if you have any questions, dont make assumes and take time to understand how fetch modules work.

If you need to read any other files, let me know.