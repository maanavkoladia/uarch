@lib/STDCells/STDCell_Macros.vh  
@rtl/defines/interconnect_define.vh  
@rtl/pkgs/interconnect_pkg.sv  
@rtl/defines/common_define.vh  
@rtl/pkgs/common_pkg.sv
@rtl/DCache/

look at the .sv files in the dcache folder. I need these sv files ported to structural 2005-verilog using the macros defined in the STDCell_Macros.vh.

Put all structural implementations of each module into the structural folder in the dcache directory.

Let me know which macros need to be implement if you need any beyond the std macro lib. Do not implement them yourself I will implement them for you. 

Take extra time to fully understand how the dcache functions and interacts with the rest of the system. 

Within the dcache folder is a gen file that contains the fsm that runs the dcache (dcache bank controller and vcache bank controller).

When porting to structural make sure you dont use any always block just assigns. no ternarys or logical and bitwise operators. it has to be fully gate implentations. you can only use assigns just for wires. 

Also make sure to understand the dcache common package.

General guidance on the dcache structure:
There is 1 dcache top that instantiates 4 dcache blocks and 1 MIO block.
The MIO block itself has no further hierarchy, there is jsut one file.
The Dcache block each intantiate a dcache bank, vcache, and an eviction buffers.
The Dcache banks have 8 direct mapped cache lines, the vcache has 4 fully associative cache lines.

to get data from the victim to the dcache and vice versa ther are swap buffers.

The vcache uses eviction buffers for evicting data to main mem if the line is dirty.

the dcache and vcache both uses ram cells and the eviction buffers are just registers.
the dcache and vcache both use a phased clock to write into the ram cells from memory.

Plan a way to port dcache and make sure you understand how to unpack structs and properly implent the dcache logic. 

Let me know if you have any questions, dont make assumptions and take time to understand how dcache works.


I need the eviction buffer to be ported to structural verilog 2005.
I need to update the instantiation in the dcache block module. 

Create as many ports as you. do not just create large wires for a struct.
However, a cache line can be one wire but an entre structure should not just be an entire wire. 
It should be readable. 

The goal here is to slowly integrate each module structurally so it is important that everytime we add a structural module we dont remove the old structs that the sv verion was using
instead I want to make sure it is easy to switch back and forth between structuarl and syste versions.
this means in dcache top I to have the structs wired in and instaniated as needed as well as accesse to just the wires so I can easily swap in and out modules

Take time to understand dcache. Becareful of logical loops that will happen due to switching from 0 time delay system to structural.

remember all I need is an updated dcacheTOP.sv that is WITHIN the structuarl folder only touch files or bring in files in the structural folder. do not alter other parts of the dcache.


This time you will be porting the vcache. Understand how the vcache.

DO NOT MESS WITH THE GEOMETRY OF THE TAG STORE.

keep all block names and gen vars i.e g_* should be kept the same in the structural models. also keep the module name the same DONOT put an _struct.

Last time a bug that You made was a simple variable name swap. always double check that the code exactly matches the system verilog reference.








