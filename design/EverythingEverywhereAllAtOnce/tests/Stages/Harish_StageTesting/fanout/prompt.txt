I need to create a plan on how to address fanout. I have a script that finds fanout 
within a module. 
and the goal is the find a way to address the fanout iwth the least 
amount of timing added as possible. 
here is the info I will pass. I will be passing in a a fanout folder for 
the module that i want you to fix violations for. right now we are fixing violaotions
for the top mem module which contains icachce/dcache/mainmem/busArb/dma/ddr5

right now you will work on fixing icache. It is very important to minimize. we are not worryied about power or area we are oly care about frequency and spcifiy our icache
is very tuned to work at 8ns. adding buffers is not trivial as it could potentially add to a critical path
it is better if we duplicate regisete where possible
we do not care about area so adding registesr is not  problem.
I need a tool that can find the optimal place of where to add registers (that are duplicates) of existing registers
or fanout wires at minimal cost to the critical path. 
if no such tools exist create a script to do so.

So in this test bench we have instantiated memory top module so everything that tocuhes icache from the mem standpoint will be here. 
there are a few more signals from fetch taht we cant bring in at the moment.
Create a plan as to how to address the problem
I have also passed in the buffer lib list. notice how inverting buffers are very cheap compared to regular buffers
and buffer timings do not scale perfectly linearly as we add more drive power. with higher buffering its cheaper per buffer count
I have already changed my inverter macro to use the 16 drive bufferIv16 module.

So overall. undersatnd how the script identifies timing violations. then create a tool or use a tool to find the optimal place to address fanout
In order to do this you will need to understand the cost of adding each buffer and understand how cheap adding a register can be.

Create a plan.