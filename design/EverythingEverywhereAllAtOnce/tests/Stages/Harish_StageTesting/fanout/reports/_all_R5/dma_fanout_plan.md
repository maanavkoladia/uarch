# Fanout report -- module `dma`

_Generated by `tests/Stages/Harish_StageTesting/fanout/scripts/fanout_optimize.py`._

> **Note on `inv_N$`**: as of this report, `lib/STDCells/UniqueLib.v` still wraps `inv1$` (drive ~1) inside `inv_N$`, **not** `bufferHInv16$`. Many recommendations assume the macro will be upgraded. If the macro has already been changed in another branch, ignore this note. Otherwise, either upgrade the macro globally (clears every inverter-driven violation at once) or instantiate `bufferHInv*$` explicitly per violation.

## Summary

- Total VIOLATIONs in check.log: **358**
- In-scope (driver inside `dma`): **29**
- Cross-module (driver outside but module name suggests `dma` loads): **8**
- Tristate-bus mismatches: **0**
- Boundary WARNINGs touching `dma`: **0**
- Over-buffered INFOs in scope: **3**

### In-scope driver-class breakdown

| Driver cell | Count | Default strategy |
|---|---|---|
| `mux2$` | 24 | gate replication (0 ns) or buffer (tier delay) |
| `reg64e$` | 3 | register duplication (0 ns) |
| `and4$` | 2 | gate replication (0 ns) or buffer (tier delay) |

---

## In-scope violations (29)

### 1. `q[31]` (fanout=7) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → u_counter_reg(MPS_reg_rst_we$_WIDTH32) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DMA_Controller>` clone `u_counter_reg` into 2 copies (`u_counter_reg_a` … `u_counter_reg_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `u_counter_reg.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `u_counter_reg.q` and its loads in `<file for DMA_Controller>`. The buffer's input is a single load on `u_counter_reg.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 2. `q[0]` (fanout=12) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → dma_fsm_unit(DMA_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DMA_FSM>` clone `ff_0` into 3 copies (`ff_0_a` … `ff_0_c`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `<file for DMA_FSM>`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 3. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → dma_fsm_unit(DMA_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DMA_FSM>` clone `ff_1` into 3 copies (`ff_1_a` … `ff_1_c`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DMA_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 4. `out` (fanout=33) — driven by `and4$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → dma_fsm_unit(DMA_FSM) → ld_counter_o_and(and4_N$_WIDTH1) → g[0].u(MPS_AND_IN4) → g0(and4$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.24 ns | In `<file for DMA_FSM>` instantiate `ld_counter_o_and` (a `and4$`-class gate) 3 times with the same fanin; split loads ~11 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `ld_counter_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 11 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `ld_counter_o_and` now has +2 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_FSM>`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `ld_counter_o_and.out` in `<file for DMA_FSM>`; routes all 33 loads through one buffer. The buffer's input is a fanout-1 load on `ld_counter_o_and.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `ld_counter_o_and.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 5. `out` (fanout=19) — driven by `and4$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → dma_fsm_unit(DMA_FSM) → ld_writeBuf_o_and(and4_N$_WIDTH1) → g[0].u(MPS_AND_IN4) → g0(and4$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.24 ns | In `<file for DMA_FSM>` instantiate `ld_writeBuf_o_and` (a `and4$`-class gate) 2 times with the same fanin; split loads ~10 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `ld_writeBuf_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 10 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `ld_writeBuf_o_and` now has +1 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_FSM>`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `ld_writeBuf_o_and.out` in `<file for DMA_FSM>`; routes all 19 loads through one buffer. The buffer's input is a fanout-1 load on `ld_writeBuf_o_and.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `ld_writeBuf_o_and.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 6. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[15].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[15].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[15].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[15].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[15].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[15].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[15].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 7. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[14].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[14].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[14].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[14].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[14].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[14].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[14].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 8. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[13].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[13].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[13].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[13].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[13].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[13].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[13].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 9. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[12].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[12].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[12].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[12].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[12].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[12].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[12].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 10. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[11].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[11].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[11].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[11].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[11].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[11].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[11].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 11. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[10].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[10].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[10].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[10].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[10].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[10].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[10].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 12. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[9].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[9].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[9].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[9].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[9].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[9].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[9].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 13. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[8].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[8].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[8].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[8].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[8].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[8].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[8].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 14. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[7].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[7].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[7].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[7].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[7].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[7].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[7].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 15. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[6].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[6].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[6].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[6].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[6].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[6].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[6].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 16. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[5].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[5].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[5].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[5].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[5].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[5].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[5].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 17. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[4].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[4].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[4].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[4].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[4].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[4].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[4].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 18. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[3].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[3].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[3].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[3].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[3].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[3].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[3].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 19. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[2].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[2].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[2].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[2].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[2].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[2].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[2].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 20. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[1].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[1].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[1].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[1].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[1].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[1].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[1].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 21. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → g_vmask[0].u_vm(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `g_vmask[0].u_vm` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `g_vmask[0].u_vm` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `g_vmask[0].u_vm` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `g_vmask[0].u_vm.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `g_vmask[0].u_vm.out` in `<file for DMA_Controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `g_vmask[0].u_vm.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 22. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → u_wc_add(kogge_stone_adder_WIDTH32) → gen_stage[1].gen_bit[1].g_active.g_gray.u_gray(gray_cell) → u_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `u_wc_add` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_wc_add` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_wc_add` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_wc_add.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_wc_add.out` in `<file for DMA_Controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_wc_add.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 23. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → u_wc_add(kogge_stone_adder_WIDTH32) → u_g0_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `u_wc_add` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_wc_add` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_wc_add` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_wc_add.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_wc_add.out` in `<file for DMA_Controller>`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_wc_add.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 24. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → u_counter_add(kogge_stone_adder_WIDTH32) → gen_stage[1].gen_bit[1].g_active.g_gray.u_gray(gray_cell) → u_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `u_counter_add` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_counter_add` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_counter_add` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_counter_add.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_counter_add.out` in `<file for DMA_Controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_counter_add.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 25. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → u_counter_add(kogge_stone_adder_WIDTH32) → u_g0_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `u_counter_add` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_counter_add` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_counter_add` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_counter_add.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_counter_add.out` in `<file for DMA_Controller>`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_counter_add.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 26. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → u_diff_calc(kogge_stone_adder_WIDTH32) → gen_stage[1].gen_bit[1].g_active.g_gray.u_gray(gray_cell) → u_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `u_diff_calc` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_diff_calc` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_diff_calc` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_diff_calc.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_diff_calc.out` in `<file for DMA_Controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_diff_calc.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 27. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → u_diff_calc(kogge_stone_adder_WIDTH32) → u_g0_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `u_diff_calc` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_diff_calc` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_diff_calc` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_diff_calc.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_diff_calc.out` in `<file for DMA_Controller>`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_diff_calc.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 28. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → u_addr_add(kogge_stone_adder_WIDTH32) → gen_stage[1].gen_bit[1].g_active.g_gray.u_gray(gray_cell) → u_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `u_addr_add` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_addr_add` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_addr_add` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_addr_add.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_addr_add.out` in `<file for DMA_Controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_addr_add.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 29. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → dma_controller_unit(DMA_Controller) → u_addr_add(kogge_stone_adder_WIDTH32) → u_g0_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DMA_Controller>` instantiate `u_addr_add` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_addr_add` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_addr_add` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DMA_Controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_addr_add.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_addr_add.out` in `<file for DMA_Controller>`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_addr_add.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

---

## Cross-module violations affecting `dma` (8)

Driver lives outside the requested module but the driver-side module name suggests loads in `dma`. Recommended fix is normally on the driver side; alternatively, re-buffer at the `dma` input port.

### 1. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_3_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

### 2. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_2_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

### 3. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_1_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

### 4. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_0_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

### 5. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → core_2_dma_fsm(DTE_Core_2_DMA_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 6. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 7. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 8. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

---

## Over-buffered INFOs (3)

Buffer rule satisfied with stronger drive than required; valid but wasteful. Optional cleanup.

- `out[0]` (fanout=16) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
