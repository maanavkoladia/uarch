# Fanout report -- module `busArb`

_Generated by `tests/Stages/Harish_StageTesting/fanout/scripts/fanout_optimize.py`._

> **Note on `inv_N$`**: as of this report, `lib/STDCells/UniqueLib.v` still wraps `inv1$` (drive ~1) inside `inv_N$`, **not** `bufferHInv16$`. Many recommendations assume the macro will be upgraded. If the macro has already been changed in another branch, ignore this note. Otherwise, either upgrade the macro globally (clears every inverter-driven violation at once) or instantiate `bufferHInv*$` explicitly per violation.

## Summary

- Total VIOLATIONs in check.log: **363**
- In-scope (driver inside `busArb`): **62**
- Cross-module (driver outside but module name suggests `busArb` loads): **0**
- Tristate-bus mismatches: **0**
- Boundary WARNINGs touching `busArb`: **0**
- Over-buffered INFOs in scope: **36**

### In-scope driver-class breakdown

| Driver cell | Count | Default strategy |
|---|---|---|
| `reg64e$` | 40 | register duplication (0 ns) |
| `nand3$` | 7 | gate replication (0 ns) or buffer (tier delay) |
| `nand4$` | 5 | gate replication (0 ns) or buffer (tier delay) |
| `and3$` | 4 | gate replication (0 ns) or buffer (tier delay) |
| `mux2$` | 4 | gate replication (0 ns) or buffer (tier delay) |
| `or3$` | 1 | gate replication (0 ns) or buffer (tier delay) |
| `decoder2_4$` | 1 | buffer (tier delay) |

---

## In-scope violations (62)

### 1. `out[0]` (fanout=9) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → u_or_ld_req(or5_N$_WIDTH1) → g_or5[0].u0(MPS_OR_IN5$) → g3(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.00 ns | In `<file for DTE>` instantiate `u_or_ld_req` (a `nand3$`-class gate) 3 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_or_ld_req` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_or_ld_req` now has +2 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_or_ld_req.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_or_ld_req.out` in `<file for DTE>`; routes all 9 loads through one buffer. The buffer's input is a fanout-1 load on `u_or_ld_req.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 2. `out[0]` (fanout=7) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → u_or_st_req(or5_N$_WIDTH1) → g_or5[0].u0(MPS_OR_IN5$) → g3(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DTE>` instantiate `u_or_st_req` (a `nand3$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_or_st_req` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_or_st_req` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_or_st_req.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_or_st_req.out` in `<file for DTE>`; routes all 7 loads through one buffer. The buffer's input is a fanout-1 load on `u_or_st_req.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 3. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_0(DTE_MEM_2_DCache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` instantiate `mem_valid_o_nand` (a `nand3$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `mem_valid_o_nand` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `mem_valid_o_nand` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_MEM_2_DCache_FSM>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `mem_valid_o_nand.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `mem_valid_o_nand.out` in `<file for DTE_MEM_2_DCache_FSM>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `mem_valid_o_nand.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 4. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_1(DTE_MEM_2_DCache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` instantiate `mem_valid_o_nand` (a `nand3$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `mem_valid_o_nand` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `mem_valid_o_nand` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_MEM_2_DCache_FSM>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `mem_valid_o_nand.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `mem_valid_o_nand.out` in `<file for DTE_MEM_2_DCache_FSM>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `mem_valid_o_nand.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 5. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_2(DTE_MEM_2_DCache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` instantiate `mem_valid_o_nand` (a `nand3$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `mem_valid_o_nand` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `mem_valid_o_nand` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_MEM_2_DCache_FSM>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `mem_valid_o_nand.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `mem_valid_o_nand.out` in `<file for DTE_MEM_2_DCache_FSM>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `mem_valid_o_nand.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 6. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_3(DTE_MEM_2_DCache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` instantiate `mem_valid_o_nand` (a `nand3$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `mem_valid_o_nand` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `mem_valid_o_nand` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_MEM_2_DCache_FSM>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `mem_valid_o_nand.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `mem_valid_o_nand.out` in `<file for DTE_MEM_2_DCache_FSM>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `mem_valid_o_nand.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 7. `out[0]` (fanout=16) — driven by `nand4$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_0(DTE_MEM_2_DCache_FSM) → Drive_Addr_Bus_o_nand(nand4_N$_WIDTH1) → g[0].u(nand4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 4 (or duplicate source register × 4) | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` instantiate `Drive_Addr_Bus_o_nand` (a `nand4$`-class gate) 4 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `Drive_Addr_Bus_o_nand` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `Drive_Addr_Bus_o_nand` now has +3 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_MEM_2_DCache_FSM>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `Drive_Addr_Bus_o_nand.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `Drive_Addr_Bus_o_nand.out` in `<file for DTE_MEM_2_DCache_FSM>`; routes all 16 loads through one buffer. The buffer's input is a fanout-1 load on `Drive_Addr_Bus_o_nand.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 8. `out[0]` (fanout=16) — driven by `nand4$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_1(DTE_MEM_2_DCache_FSM) → Drive_Addr_Bus_o_nand(nand4_N$_WIDTH1) → g[0].u(nand4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 4 (or duplicate source register × 4) | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` instantiate `Drive_Addr_Bus_o_nand` (a `nand4$`-class gate) 4 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `Drive_Addr_Bus_o_nand` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `Drive_Addr_Bus_o_nand` now has +3 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_MEM_2_DCache_FSM>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `Drive_Addr_Bus_o_nand.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `Drive_Addr_Bus_o_nand.out` in `<file for DTE_MEM_2_DCache_FSM>`; routes all 16 loads through one buffer. The buffer's input is a fanout-1 load on `Drive_Addr_Bus_o_nand.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 9. `out[0]` (fanout=16) — driven by `nand4$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_2(DTE_MEM_2_DCache_FSM) → Drive_Addr_Bus_o_nand(nand4_N$_WIDTH1) → g[0].u(nand4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 4 (or duplicate source register × 4) | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` instantiate `Drive_Addr_Bus_o_nand` (a `nand4$`-class gate) 4 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `Drive_Addr_Bus_o_nand` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `Drive_Addr_Bus_o_nand` now has +3 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_MEM_2_DCache_FSM>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `Drive_Addr_Bus_o_nand.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `Drive_Addr_Bus_o_nand.out` in `<file for DTE_MEM_2_DCache_FSM>`; routes all 16 loads through one buffer. The buffer's input is a fanout-1 load on `Drive_Addr_Bus_o_nand.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 10. `out[0]` (fanout=16) — driven by `nand4$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_3(DTE_MEM_2_DCache_FSM) → Drive_Addr_Bus_o_nand(nand4_N$_WIDTH1) → g[0].u(nand4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 4 (or duplicate source register × 4) | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` instantiate `Drive_Addr_Bus_o_nand` (a `nand4$`-class gate) 4 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `Drive_Addr_Bus_o_nand` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `Drive_Addr_Bus_o_nand` now has +3 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_MEM_2_DCache_FSM>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `Drive_Addr_Bus_o_nand.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `Drive_Addr_Bus_o_nand.out` in `<file for DTE_MEM_2_DCache_FSM>`; routes all 16 loads through one buffer. The buffer's input is a fanout-1 load on `Drive_Addr_Bus_o_nand.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 11. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DTE_MEM_2_ICache_FSM>` instantiate `mem_valid_o_nand` (a `nand3$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `mem_valid_o_nand` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `mem_valid_o_nand` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_MEM_2_ICache_FSM>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `mem_valid_o_nand.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `mem_valid_o_nand.out` in `<file for DTE_MEM_2_ICache_FSM>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `mem_valid_o_nand.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 12. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_3_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.24 ns | In `<file for DTE_DMA_2_MEM_FSM>` instantiate `Drv_DB_3_o_and` (a `and3$`-class gate) 3 times with the same fanin; split loads ~11 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `Drv_DB_3_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 11 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `Drv_DB_3_o_and` now has +2 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_DMA_2_MEM_FSM>`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `Drv_DB_3_o_and.out` in `<file for DTE_DMA_2_MEM_FSM>`; routes all 33 loads through one buffer. The buffer's input is a fanout-1 load on `Drv_DB_3_o_and.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `Drv_DB_3_o_and.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 13. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_2_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.24 ns | In `<file for DTE_DMA_2_MEM_FSM>` instantiate `Drv_DB_2_o_and` (a `and3$`-class gate) 3 times with the same fanin; split loads ~11 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `Drv_DB_2_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 11 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `Drv_DB_2_o_and` now has +2 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_DMA_2_MEM_FSM>`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `Drv_DB_2_o_and.out` in `<file for DTE_DMA_2_MEM_FSM>`; routes all 33 loads through one buffer. The buffer's input is a fanout-1 load on `Drv_DB_2_o_and.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `Drv_DB_2_o_and.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 14. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_1_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.24 ns | In `<file for DTE_DMA_2_MEM_FSM>` instantiate `Drv_DB_1_o_and` (a `and3$`-class gate) 3 times with the same fanin; split loads ~11 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `Drv_DB_1_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 11 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `Drv_DB_1_o_and` now has +2 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_DMA_2_MEM_FSM>`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `Drv_DB_1_o_and.out` in `<file for DTE_DMA_2_MEM_FSM>`; routes all 33 loads through one buffer. The buffer's input is a fanout-1 load on `Drv_DB_1_o_and.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `Drv_DB_1_o_and.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 15. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_0_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.24 ns | In `<file for DTE_DMA_2_MEM_FSM>` instantiate `Drv_DB_0_o_and` (a `and3$`-class gate) 3 times with the same fanin; split loads ~11 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `Drv_DB_0_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 11 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `Drv_DB_0_o_and` now has +2 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE_DMA_2_MEM_FSM>`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `Drv_DB_0_o_and.out` in `<file for DTE_DMA_2_MEM_FSM>`; routes all 33 loads through one buffer. The buffer's input is a fanout-1 load on `Drv_DB_0_o_and.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `Drv_DB_0_o_and.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 16. `q[3]` (fanout=5) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → lat_best_pick(MPS_reg_rst_we$_WIDTH4) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for BusArbitration>` clone `lat_best_pick` into 2 copies (`lat_best_pick_a` … `lat_best_pick_b`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `lat_best_pick.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `lat_best_pick.q` and its loads in `<file for BusArbitration>`. The buffer's input is a single load on `lat_best_pick.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 17. `q[3]` (fanout=5) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → lat_ic_req(MPS_reg_rst_we$_WIDTH4) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for Scheduler>` clone `lat_ic_req` into 2 copies (`lat_ic_req_a` … `lat_ic_req_b`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `lat_ic_req.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `lat_ic_req.q` and its loads in `<file for Scheduler>`. The buffer's input is a single load on `lat_ic_req.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 18. `q[3]` (fanout=6) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → lat_dc_req_0(MPS_reg_rst_we$_WIDTH4) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for Scheduler>` clone `lat_dc_req_0` into 2 copies (`lat_dc_req_0_a` … `lat_dc_req_0_b`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `lat_dc_req_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `lat_dc_req_0.q` and its loads in `<file for Scheduler>`. The buffer's input is a single load on `lat_dc_req_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 19. `q[3]` (fanout=6) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → lat_dc_req_1(MPS_reg_rst_we$_WIDTH4) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for Scheduler>` clone `lat_dc_req_1` into 2 copies (`lat_dc_req_1_a` … `lat_dc_req_1_b`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `lat_dc_req_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `lat_dc_req_1.q` and its loads in `<file for Scheduler>`. The buffer's input is a single load on `lat_dc_req_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 20. `q[3]` (fanout=6) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → lat_dc_req_2(MPS_reg_rst_we$_WIDTH4) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for Scheduler>` clone `lat_dc_req_2` into 2 copies (`lat_dc_req_2_a` … `lat_dc_req_2_b`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `lat_dc_req_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `lat_dc_req_2.q` and its loads in `<file for Scheduler>`. The buffer's input is a single load on `lat_dc_req_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 21. `q[3]` (fanout=6) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → lat_dc_req_3(MPS_reg_rst_we$_WIDTH4) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for Scheduler>` clone `lat_dc_req_3` into 2 copies (`lat_dc_req_3_a` … `lat_dc_req_3_b`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `lat_dc_req_3.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `lat_dc_req_3.q` and its loads in `<file for Scheduler>`. The buffer's input is a single load on `lat_dc_req_3.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 22. `q[7]` (fanout=5) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → lat_wbV(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for Scheduler>` clone `lat_wbV` into 2 copies (`lat_wbV_a` … `lat_wbV_b`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `lat_wbV.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `lat_wbV.q` and its loads in `<file for Scheduler>`. The buffer's input is a single load on `lat_wbV.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 23. `out` (fanout=12) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → u_md_req_hit(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.00 ns | In `<file for DTE>` instantiate `u_md_req_hit` (a `mux2$`-class gate) 3 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_md_req_hit` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_md_req_hit` now has +2 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_md_req_hit.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_md_req_hit.out` in `<file for DTE>`; routes all 12 loads through one buffer. The buffer's input is a fanout-1 load on `u_md_req_hit.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 24. `out` (fanout=8) — driven by `or3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → u_dm_req_hit(or3_N$_WIDTH1) → g_or3[0].u0(MPS_OR_IN3$) → g0(or3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for DTE>` instantiate `u_dm_req_hit` (a `or3$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_dm_req_hit` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_dm_req_hit` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_dm_req_hit.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_dm_req_hit.out` in `<file for DTE>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_dm_req_hit.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 25. `out[3]` (fanout=5) — driven by `decoder2_4$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → u_bk_dec(MPS_decoder$_INPUTS2) → GEN_DEC2.u_dec(decoder2_4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Insert bufferH16$ | 0.24 ns | Driver class for `decoder2_4$` not specifically modeled. Default: insert `bufferH16$` (0.24 ns). |

### 26. `out[0]` (fanout=13) — driven by `nand4$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → u_nand_dte_busy(nand4_N$_WIDTH1) → g[0].u(nand4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 4 (or duplicate source register × 4) | 0.00 ns | In `<file for DTE>` instantiate `u_nand_dte_busy` (a `nand4$`-class gate) 4 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_nand_dte_busy` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_nand_dte_busy` now has +3 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for DTE>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_nand_dte_busy.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_nand_dte_busy.out` in `<file for DTE>`; routes all 13 loads through one buffer. The buffer's input is a fanout-1 load on `u_nand_dte_busy.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 27. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_ICache_FSM>` clone `ff_0` into 3 copies (`ff_0_a` … `ff_0_c`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `<file for DTE_MEM_2_ICache_FSM>`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 28. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_ICache_FSM>` clone `ff_1` into 3 copies (`ff_1_a` … `ff_1_c`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_MEM_2_ICache_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 29. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_ICache_FSM>` clone `ff_2` into 3 copies (`ff_2_a` … `ff_2_c`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_2.q` and its loads in `<file for DTE_MEM_2_ICache_FSM>`. The buffer's input is a single load on `ff_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 30. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_0(DTE_MEM_2_DCache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_0` into 3 copies (`ff_0_a` … `ff_0_c`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 31. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_0(DTE_MEM_2_DCache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_1` into 3 copies (`ff_1_a` … `ff_1_c`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 32. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_0(DTE_MEM_2_DCache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_2` into 3 copies (`ff_2_a` … `ff_2_c`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_2.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 33. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_1(DTE_MEM_2_DCache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_0` into 3 copies (`ff_0_a` … `ff_0_c`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 34. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_1(DTE_MEM_2_DCache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_1` into 3 copies (`ff_1_a` … `ff_1_c`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 35. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_1(DTE_MEM_2_DCache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_2` into 3 copies (`ff_2_a` … `ff_2_c`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_2.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 36. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_2(DTE_MEM_2_DCache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_0` into 3 copies (`ff_0_a` … `ff_0_c`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 37. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_2(DTE_MEM_2_DCache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_1` into 3 copies (`ff_1_a` … `ff_1_c`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 38. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_2(DTE_MEM_2_DCache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_2` into 3 copies (`ff_2_a` … `ff_2_c`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_2.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 39. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_3(DTE_MEM_2_DCache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_0` into 3 copies (`ff_0_a` … `ff_0_c`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 40. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_3(DTE_MEM_2_DCache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_1` into 3 copies (`ff_1_a` … `ff_1_c`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 41. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_3(DTE_MEM_2_DCache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `<file for DTE_MEM_2_DCache_FSM>` clone `ff_2` into 3 copies (`ff_2_a` … `ff_2_c`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_2.q` and its loads in `<file for DTE_MEM_2_DCache_FSM>`. The buffer's input is a single load on `ff_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 42. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_0(DTE_DCache_2_MEM_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_0` into 2 copies (`ff_0_a` … `ff_0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 43. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_0(DTE_DCache_2_MEM_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_1` into 2 copies (`ff_1_a` … `ff_1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 44. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_0(DTE_DCache_2_MEM_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_2` into 2 copies (`ff_2_a` … `ff_2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_2.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 45. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_1(DTE_DCache_2_MEM_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_0` into 2 copies (`ff_0_a` … `ff_0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 46. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_1(DTE_DCache_2_MEM_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_1` into 2 copies (`ff_1_a` … `ff_1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 47. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_1(DTE_DCache_2_MEM_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_2` into 2 copies (`ff_2_a` … `ff_2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_2.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 48. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_2(DTE_DCache_2_MEM_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_0` into 2 copies (`ff_0_a` … `ff_0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 49. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_2(DTE_DCache_2_MEM_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_1` into 2 copies (`ff_1_a` … `ff_1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 50. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_2(DTE_DCache_2_MEM_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_2` into 2 copies (`ff_2_a` … `ff_2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_2.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 51. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_3(DTE_DCache_2_MEM_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_0` into 2 copies (`ff_0_a` … `ff_0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 52. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_3(DTE_DCache_2_MEM_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_1` into 2 copies (`ff_1_a` … `ff_1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 53. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_3(DTE_DCache_2_MEM_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DCache_2_MEM_FSM>` clone `ff_2` into 2 copies (`ff_2_a` … `ff_2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_2.q` and its loads in `<file for DTE_DCache_2_MEM_FSM>`. The buffer's input is a single load on `ff_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 54. `q[0]` (fanout=7) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → ddr5_2_core_fsm(DTE_DDR5_2_Core_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DDR5_2_Core_FSM>` clone `ff_1` into 2 copies (`ff_1_a` … `ff_1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_DDR5_2_Core_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 55. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → core_2_ddr5_fsm(DTE_Core_2_DDR5_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_Core_2_DDR5_FSM>` clone `ff_1` into 2 copies (`ff_1_a` … `ff_1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_Core_2_DDR5_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 56. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → core_2_dma_fsm(DTE_Core_2_DMA_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_Core_2_DMA_FSM>` clone `ff_1` into 2 copies (`ff_1_a` … `ff_1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_Core_2_DMA_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 57. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DMA_2_MEM_FSM>` clone `ff_0` into 2 copies (`ff_0_a` … `ff_0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `<file for DTE_DMA_2_MEM_FSM>`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 58. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DMA_2_MEM_FSM>` clone `ff_1` into 2 copies (`ff_1_a` … `ff_1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `<file for DTE_DMA_2_MEM_FSM>`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 59. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for DTE_DMA_2_MEM_FSM>` clone `ff_2` into 2 copies (`ff_2_a` … `ff_2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_2.q` and its loads in `<file for DTE_DMA_2_MEM_FSM>`. The buffer's input is a single load on `ff_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 60. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → dcache_picking_unit(Scheduler_DCachePicking) → u_sub_01(kogge_stone_adder_WIDTH4) → gen_stage[2].gen_bit[3].g_active.g_gray.u_gray(gray_cell) → u_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for Scheduler_DCachePicking>` instantiate `u_sub_01` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_sub_01` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_sub_01` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for Scheduler_DCachePicking>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_sub_01.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_sub_01.out` in `<file for Scheduler_DCachePicking>`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_sub_01.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 61. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → dcache_picking_unit(Scheduler_DCachePicking) → u_sub_23(kogge_stone_adder_WIDTH4) → gen_stage[2].gen_bit[3].g_active.g_gray.u_gray(gray_cell) → u_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for Scheduler_DCachePicking>` instantiate `u_sub_23` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_sub_23` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_sub_23` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for Scheduler_DCachePicking>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_sub_23.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_sub_23.out` in `<file for Scheduler_DCachePicking>`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_sub_23.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 62. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → dcache_picking_unit(Scheduler_DCachePicking) → u_sub_final(kogge_stone_adder_WIDTH4) → gen_stage[2].gen_bit[3].g_active.g_gray.u_gray(gray_cell) → u_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for Scheduler_DCachePicking>` instantiate `u_sub_final` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_sub_final` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_sub_final` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for Scheduler_DCachePicking>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_sub_final.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_sub_final.out` in `<file for Scheduler_DCachePicking>`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_sub_final.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

---

## Over-buffered INFOs (36)

Buffer rule satisfied with stronger drive than required; valid but wasteful. Optional cleanup.

- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=8) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=12) driven by `bufferHInv64$`
- `out[0]` (fanout=8) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=15) driven by `bufferHInv64$`
- `out[0]` (fanout=8) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=15) driven by `bufferHInv64$`
- `out[0]` (fanout=8) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=15) driven by `bufferHInv64$`
- `out[0]` (fanout=8) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=15) driven by `bufferHInv64$`
- `out[0]` (fanout=9) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=9) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=9) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=9) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=6) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=9) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
