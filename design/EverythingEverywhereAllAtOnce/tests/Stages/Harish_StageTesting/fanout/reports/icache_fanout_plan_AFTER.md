# Fanout report -- module `icache`

_Generated by `tests/Stages/Harish_StageTesting/fanout/scripts/fanout_optimize.py`._

> **Note on `inv_N$`**: as of this report, `lib/STDCells/UniqueLib.v` still wraps `inv1$` (drive ~1) inside `inv_N$`, **not** `bufferHInv16$`. Many recommendations assume the macro will be upgraded. If the macro has already been changed in another branch, ignore this note. Otherwise, either upgrade the macro globally (clears every inverter-driven violation at once) or instantiate `bufferHInv*$` explicitly per violation.

## Summary

- Total VIOLATIONs in check.log: **1148**
- In-scope (driver inside `icache`): **12**
- Cross-module (driver outside but module name suggests `icache` loads): **4**
- Tristate-bus mismatches: **0**
- Boundary WARNINGs touching `icache`: **0**
- Over-buffered INFOs in scope: **2**

### In-scope driver-class breakdown

| Driver cell | Count | Default strategy |
|---|---|---|
| `mux2$` | 10 | gate replication (0 ns) or buffer (tier delay) |
| `bufferH64$` | 1 | downsize / no action |
| `and4$` | 1 | gate replication (0 ns) or buffer (tier delay) |

---

## In-scope violations (12)

### 1. `out` (fanout=28) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux_a(mux2_N_WIDTH32) → gen_mux[7].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.24 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux_a` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~14 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux_a` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 14 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `u_vaddr_mux_a` now has +1 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `u_vaddr_mux_a.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 28 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux_a.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `u_vaddr_mux_a.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 2. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux_a(mux2_N_WIDTH32) → gen_mux[6].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux_a` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux_a` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux_a` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux_a.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux_a.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux_a.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 3. `out` (fanout=11) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux_a(mux2_N_WIDTH32) → gen_mux[5].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux_a` (a `mux2$`-class gate) 3 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux_a` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux_a` now has +2 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux_a.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux_a.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 11 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux_a.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 4. `out` (fanout=11) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux_a(mux2_N_WIDTH32) → gen_mux[4].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux_a` (a `mux2$`-class gate) 3 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux_a` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux_a` now has +2 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux_a.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux_a.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 11 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux_a.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 5. `busy_o` (fanout=101) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_controller_fsm(ICache_Controller_Logic) → u_busy_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 6. `out` (fanout=5) — driven by `and4$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_controller_fsm(ICache_Controller_Logic) → Fill3EN_o_and(and4_N$_WIDTH1) → g[0].u(MPS_AND_IN4) → g0(and4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv` instantiate `Fill3EN_o_and` (a `and4$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `Fill3EN_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `Fill3EN_o_and` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `Fill3EN_o_and.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `Fill3EN_o_and.out` in `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `Fill3EN_o_and.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 7. `out` (fanout=133) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux_b(mux2_N_WIDTH32) → gen_mux[7].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.30 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux_b` (a `mux2$`-class gate) 3 times with the same fanin; split loads ~45 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux_b` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 45 → `bufferH64$` (0.30 ns). **Input side**: each fan-in wire of `u_vaddr_mux_b` now has +2 extra loads → no input buffer. Total added delay = 0.30 + 0.00 = **0.30 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv256$ (polarity flip needed) | 0.45 ns | Use `bufferHInv256$` (inverting, **0.45 ns**) only if every consumer of `u_vaddr_mux_b.out` can absorb the inversion. Pair cost = 0.90 ns. |
| alt 2 | Buffer with bufferH256$ | 0.54 ns | Insert `bufferH256$` after `u_vaddr_mux_b.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 133 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux_b.out` (no upstream cascade). Adds **0.54 ns** to every load path. |

### 8. `out` (fanout=32) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux_b(mux2_N_WIDTH32) → gen_mux[6].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.24 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux_b` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~16 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux_b` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 16 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `u_vaddr_mux_b` now has +1 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `u_vaddr_mux_b.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 32 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux_b.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `u_vaddr_mux_b.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 9. `out` (fanout=32) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux_b(mux2_N_WIDTH32) → gen_mux[5].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.24 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux_b` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~16 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux_b` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 16 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `u_vaddr_mux_b` now has +1 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `u_vaddr_mux_b.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 32 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux_b.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `u_vaddr_mux_b.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 10. `out` (fanout=32) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux_b(mux2_N_WIDTH32) → gen_mux[4].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.24 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux_b` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~16 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux_b` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 16 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `u_vaddr_mux_b` now has +1 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `u_vaddr_mux_b.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 32 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux_b.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `u_vaddr_mux_b.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 11. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_TagStore_unit(ICache_TagStore) → u_we_valid_base_a(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_TagStore_structural.v` instantiate `u_we_valid_base_a` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_we_valid_base_a` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_we_valid_base_a` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_TagStore_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_we_valid_base_a.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_we_valid_base_a.out` in `rtl/ICache/structural/fanout/ICache_TagStore_structural.v`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_we_valid_base_a.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 12. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_TagStore_unit(ICache_TagStore) → u_we_valid_base_b(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_TagStore_structural.v` instantiate `u_we_valid_base_b` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_we_valid_base_b` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_we_valid_base_b` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_TagStore_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_we_valid_base_b.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_we_valid_base_b.out` in `rtl/ICache/structural/fanout/ICache_TagStore_structural.v`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_we_valid_base_b.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

---

## Cross-module violations affecting `icache` (4)

Driver lives outside the requested module but the driver-side module name suggests loads in `icache`. Recommended fix is normally on the driver side; alternatively, re-buffer at the `icache` input port.

### 1. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 2. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 3. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 4. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

---

## Over-buffered INFOs (2)

Buffer rule satisfied with stronger drive than required; valid but wasteful. Optional cleanup.

- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=6) driven by `bufferHInv64$`
