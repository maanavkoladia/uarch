# Fanout report -- module `icache`

_Generated by `tests/Stages/Harish_StageTesting/fanout/scripts/fanout_optimize.py`._

> **Note on `inv_N$`**: as of this report, `lib/STDCells/UniqueLib.v` still wraps `inv1$` (drive ~1) inside `inv_N$`, **not** `bufferHInv16$`. Many recommendations assume the macro will be upgraded. If the macro has already been changed in another branch, ignore this note. Otherwise, either upgrade the macro globally (clears every inverter-driven violation at once) or instantiate `bufferHInv*$` explicitly per violation.

## Summary

- Total VIOLATIONs in check.log: **1180**
- In-scope (driver inside `icache`): **44**
- Cross-module (driver outside but module name suggests `icache` loads): **4**
- Tristate-bus mismatches: **0**
- Boundary WARNINGs touching `icache`: **0**
- Over-buffered INFOs in scope: **4**

### In-scope driver-class breakdown

| Driver cell | Count | Default strategy |
|---|---|---|
| `mux2$` | 38 | gate replication (0 ns) or buffer (tier delay) |
| `reg64e$` | 3 | register duplication (0 ns) |
| `and3$` | 2 | gate replication (0 ns) or buffer (tier delay) |
| `nand3$` | 1 | gate replication (0 ns) or buffer (tier delay) |

---

## In-scope violations (44)

### 1. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_controller_fsm(ICache_Controller_Logic) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv` clone `ff_0` into 3 copies (`ff_0_a` … `ff_0_c`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_0.q` and its loads in `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv`. The buffer's input is a single load on `ff_0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 2. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_controller_fsm(ICache_Controller_Logic) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 3 | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv` clone `ff_1` into 3 copies (`ff_1_a` … `ff_1_c`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 3 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_1.q` and its loads in `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv`. The buffer's input is a single load on `ff_1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 3. `q[0]` (fanout=6) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_controller_fsm(ICache_Controller_Logic) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv` clone `ff_2` into 2 copies (`ff_2_a` … `ff_2_b`); each copy drives ~3 loads. **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `ff_2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `ff_2.q` and its loads in `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv`. The buffer's input is a single load on `ff_2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 4. `out` (fanout=134) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_controller_fsm(ICache_Controller_Logic) → RD_I_VC_SWAP_BUF_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.30 ns | In `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv` instantiate `RD_I_VC_SWAP_BUF_o_and` (a `and3$`-class gate) 3 times with the same fanin; split loads ~45 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `RD_I_VC_SWAP_BUF_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 45 → `bufferH64$` (0.30 ns). **Input side**: each fan-in wire of `RD_I_VC_SWAP_BUF_o_and` now has +2 extra loads → no input buffer. Total added delay = 0.30 + 0.00 = **0.30 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv`. |
| alt 1 | Buffer with bufferHInv256$ (polarity flip needed) | 0.45 ns | Use `bufferHInv256$` (inverting, **0.45 ns**) only if every consumer of `RD_I_VC_SWAP_BUF_o_and.out` can absorb the inversion. Pair cost = 0.90 ns. |
| alt 2 | Buffer with bufferH256$ | 0.54 ns | Insert `bufferH256$` after `RD_I_VC_SWAP_BUF_o_and.out` in `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv`; routes all 134 loads through one buffer. The buffer's input is a fanout-1 load on `RD_I_VC_SWAP_BUF_o_and.out` (no upstream cascade). Adds **0.54 ns** to every load path. |

### 5. `out[0]` (fanout=37) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_controller_fsm(ICache_Controller_Logic) → busy_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.24 ns | In `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv` instantiate `busy_o_nand` (a `nand3$`-class gate) 3 times with the same fanin; split loads ~13 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `busy_o_nand` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 13 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `busy_o_nand` now has +2 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `busy_o_nand.out` in `rtl/ICache/structural/fanout/ICache_Controller_Logic.sv`; routes all 37 loads through one buffer. The buffer's input is a fanout-1 load on `busy_o_nand.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `busy_o_nand.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 6. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[31].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 7. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[30].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 8. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[29].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 9. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[28].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 10. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[27].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 11. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[26].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 12. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[25].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 13. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[24].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 14. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[23].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 15. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[22].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 16. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[21].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 17. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[20].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 18. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[19].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 19. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[18].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 20. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[17].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 21. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[16].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 22. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[15].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 23. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[14].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 24. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[13].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 25. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[12].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 26. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[11].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 27. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[10].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 28. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[9].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 29. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[8].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_vaddr_mux` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 30. `out` (fanout=162) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[7].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.30 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 3 times with the same fanin; split loads ~54 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 54 → `bufferH64$` (0.30 ns). **Input side**: each fan-in wire of `u_vaddr_mux` now has +2 extra loads → no input buffer. Total added delay = 0.30 + 0.00 = **0.30 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv256$ (polarity flip needed) | 0.45 ns | Use `bufferHInv256$` (inverting, **0.45 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.90 ns. |
| alt 2 | Buffer with bufferH256$ | 0.54 ns | Insert `bufferH256$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 162 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.54 ns** to every load path. |

### 31. `out` (fanout=44) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[6].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.24 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 3 times with the same fanin; split loads ~15 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 15 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `u_vaddr_mux` now has +2 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 44 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 32. `out` (fanout=47) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[5].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.24 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 3 times with the same fanin; split loads ~16 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 16 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `u_vaddr_mux` now has +2 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 47 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 33. `out` (fanout=47) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_vaddr_mux(mux2_N_WIDTH32) → gen_mux[4].u_mux(MPS_MUX_IN2) → u0(mux2$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.24 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_vaddr_mux` (a `mux2$`-class gate) 3 times with the same fanin; split loads ~16 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_vaddr_mux` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 16 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `u_vaddr_mux` now has +2 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `u_vaddr_mux.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 47 loads through one buffer. The buffer's input is a fanout-1 load on `u_vaddr_mux.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `u_vaddr_mux.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 34. `out` (fanout=136) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → i_vcache_unit(I_VCache) → u_hit(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.30 ns | In `rtl/ICache/structural/fanout/I_VCache_structural.v` instantiate `u_hit` (a `and3$`-class gate) 3 times with the same fanin; split loads ~46 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_hit` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 46 → `bufferH64$` (0.30 ns). **Input side**: each fan-in wire of `u_hit` now has +2 extra loads → no input buffer. Total added delay = 0.30 + 0.00 = **0.30 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/I_VCache_structural.v`. |
| alt 1 | Buffer with bufferHInv256$ (polarity flip needed) | 0.45 ns | Use `bufferHInv256$` (inverting, **0.45 ns**) only if every consumer of `u_hit.out` can absorb the inversion. Pair cost = 0.90 ns. |
| alt 2 | Buffer with bufferH256$ | 0.54 ns | Insert `bufferH256$` after `u_hit.out` in `rtl/ICache/structural/fanout/I_VCache_structural.v`; routes all 136 loads through one buffer. The buffer's input is a fanout-1 load on `u_hit.out` (no upstream cascade). Adds **0.54 ns** to every load path. |

### 35. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → u_set_case(and2_N$_WIDTH1) → g_and_N[0].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_structural.v` instantiate `u_set_case` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_set_case` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_set_case` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_set_case.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_set_case.out` in `rtl/ICache/structural/fanout/ICache_structural.v`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_set_case.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 36. `out` (fanout=16) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_TagStore_unit(ICache_TagStore) → u_we_valid_base(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 4 (or duplicate source register × 4) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_TagStore_structural.v` instantiate `u_we_valid_base` (a `mux2$`-class gate) 4 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_we_valid_base` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_we_valid_base` now has +3 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_TagStore_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_we_valid_base.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_we_valid_base.out` in `rtl/ICache/structural/fanout/ICache_TagStore_structural.v`; routes all 16 loads through one buffer. The buffer's input is a fanout-1 load on `u_we_valid_base.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 37. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_dataStore_unit(ICache_DataStore) → u_wbw_Q3(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_DataStore_structural.v` instantiate `u_wbw_Q3` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_wbw_Q3` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_wbw_Q3` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_wbw_Q3.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_wbw_Q3.out` in `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_wbw_Q3.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 38. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_dataStore_unit(ICache_DataStore) → u_wbw_Q2(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_DataStore_structural.v` instantiate `u_wbw_Q2` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_wbw_Q2` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_wbw_Q2` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_wbw_Q2.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_wbw_Q2.out` in `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_wbw_Q2.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 39. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_dataStore_unit(ICache_DataStore) → u_wbw_Q1(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_DataStore_structural.v` instantiate `u_wbw_Q1` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_wbw_Q1` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_wbw_Q1` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_wbw_Q1.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_wbw_Q1.out` in `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_wbw_Q1.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 40. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_dataStore_unit(ICache_DataStore) → u_wbw_Q0(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_DataStore_structural.v` instantiate `u_wbw_Q0` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_wbw_Q0` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_wbw_Q0` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_wbw_Q0.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_wbw_Q0.out` in `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_wbw_Q0.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 41. `out` (fanout=16) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_dataStore_unit(ICache_DataStore) → u_sl1(and2_N$_WIDTH1) → g_and_N[0].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 4 (or duplicate source register × 4) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_DataStore_structural.v` instantiate `u_sl1` (a `mux2$`-class gate) 4 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_sl1` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_sl1` now has +3 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_sl1.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_sl1.out` in `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`; routes all 16 loads through one buffer. The buffer's input is a fanout-1 load on `u_sl1.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 42. `out` (fanout=16) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → icache_dataStore_unit(ICache_DataStore) → u_sl0(and2_N$_WIDTH1) → g_and_N[0].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 4 (or duplicate source register × 4) | 0.00 ns | In `rtl/ICache/structural/fanout/ICache_DataStore_structural.v` instantiate `u_sl0` (a `mux2$`-class gate) 4 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_sl0` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_sl0` now has +3 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_sl0.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_sl0.out` in `rtl/ICache/structural/fanout/ICache_DataStore_structural.v`; routes all 16 loads through one buffer. The buffer's input is a fanout-1 load on `u_sl0.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 43. `out` (fanout=157) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → i_vcache_unit(I_VCache) → u_hit_idx_1(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.30 ns | In `rtl/ICache/structural/fanout/I_VCache_structural.v` instantiate `u_hit_idx_1` (a `mux2$`-class gate) 3 times with the same fanin; split loads ~53 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_hit_idx_1` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 53 → `bufferH64$` (0.30 ns). **Input side**: each fan-in wire of `u_hit_idx_1` now has +2 extra loads → no input buffer. Total added delay = 0.30 + 0.00 = **0.30 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/I_VCache_structural.v`. |
| alt 1 | Buffer with bufferHInv256$ (polarity flip needed) | 0.45 ns | Use `bufferHInv256$` (inverting, **0.45 ns**) only if every consumer of `u_hit_idx_1.out` can absorb the inversion. Pair cost = 0.90 ns. |
| alt 2 | Buffer with bufferH256$ | 0.54 ns | Insert `bufferH256$` after `u_hit_idx_1.out` in `rtl/ICache/structural/fanout/I_VCache_structural.v`; routes all 157 loads through one buffer. The buffer's input is a fanout-1 load on `u_hit_idx_1.out` (no upstream cascade). Adds **0.54 ns** to every load path. |

### 44. `out` (fanout=157) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → icache_unit(ICache) → i_vcache_unit(I_VCache) → u_hit_idx_0(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 3 (or duplicate source register × 3) | 0.30 ns | In `rtl/ICache/structural/fanout/I_VCache_structural.v` instantiate `u_hit_idx_0` (a `mux2$`-class gate) 3 times with the same fanin; split loads ~53 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_hit_idx_0` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 53 → `bufferH64$` (0.30 ns). **Input side**: each fan-in wire of `u_hit_idx_0` now has +2 extra loads → no input buffer. Total added delay = 0.30 + 0.00 = **0.30 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `rtl/ICache/structural/fanout/I_VCache_structural.v`. |
| alt 1 | Buffer with bufferHInv256$ (polarity flip needed) | 0.45 ns | Use `bufferHInv256$` (inverting, **0.45 ns**) only if every consumer of `u_hit_idx_0.out` can absorb the inversion. Pair cost = 0.90 ns. |
| alt 2 | Buffer with bufferH256$ | 0.54 ns | Insert `bufferH256$` after `u_hit_idx_0.out` in `rtl/ICache/structural/fanout/I_VCache_structural.v`; routes all 157 loads through one buffer. The buffer's input is a fanout-1 load on `u_hit_idx_0.out` (no upstream cascade). Adds **0.54 ns** to every load path. |

---

## Cross-module violations affecting `icache` (4)

Driver lives outside the requested module but the driver-side module name suggests loads in `icache`. Recommended fix is normally on the driver side; alternatively, re-buffer at the `icache` input port.

### 1. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 2. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 3. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 4. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

---

## Over-buffered INFOs (4)

Buffer rule satisfied with stronger drive than required; valid but wasteful. Optional cleanup.

- `out[0]` (fanout=9) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=6) driven by `bufferHInv64$`
