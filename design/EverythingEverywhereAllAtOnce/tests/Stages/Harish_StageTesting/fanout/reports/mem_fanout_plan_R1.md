# Fanout report -- module `mem`

_Generated by `tests/Stages/Harish_StageTesting/fanout/scripts/fanout_optimize.py`._

> **Note on `inv_N$`**: as of this report, `lib/STDCells/UniqueLib.v` still wraps `inv1$` (drive ~1) inside `inv_N$`, **not** `bufferHInv16$`. Many recommendations assume the macro will be upgraded. If the macro has already been changed in another branch, ignore this note. Otherwise, either upgrade the macro globally (clears every inverter-driven violation at once) or instantiate `bufferHInv*$` explicitly per violation.

## Summary

- Total VIOLATIONs in check.log: **781**
- In-scope (driver inside `mem`): **423**
- Cross-module (driver outside but module name suggests `mem` loads): **43**
- Tristate-bus mismatches: **0**
- Boundary WARNINGs touching `mem`: **0**
- Over-buffered INFOs in scope: **193**

### In-scope driver-class breakdown

| Driver cell | Count | Default strategy |
|---|---|---|
| `reg64e$` | 136 | register duplication (0 ns) |
| `mux2$` | 83 | gate replication (0 ns) or buffer (tier delay) |
| `bufferH64$` | 64 | downsize / no action |
| `bufferHInv64$` | 64 | downsize / no action |
| `bufferH16$` | 64 | downsize / no action |
| `decoder3_8$` | 5 | buffer (tier delay) |
| `and4$` | 4 | gate replication (0 ns) or buffer (tier delay) |
| `and3$` | 2 | gate replication (0 ns) or buffer (tier delay) |
| `nand3$` | 1 | gate replication (0 ns) or buffer (tier delay) |

---

## In-scope violations (423)

### 1. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u0_fsm(mem_controller_fsm) → mem_ready_o_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller_fsm>` instantiate `mem_ready_o_or` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `mem_ready_o_or` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `mem_ready_o_or` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller_fsm>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `mem_ready_o_or.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `mem_ready_o_or.out` in `<file for mem_controller_fsm>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `mem_ready_o_or.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 2. `q[14]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_addr[7].u_ar(MPS_reg_rst_we$_WIDTH15) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_addr[7].u_ar` into 2 copies (`g_bg_addr[7].u_ar_a` … `g_bg_addr[7].u_ar_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_addr[7].u_ar.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_addr[7].u_ar.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_addr[7].u_ar.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 3. `q[14]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_addr[6].u_ar(MPS_reg_rst_we$_WIDTH15) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_addr[6].u_ar` into 2 copies (`g_bg_addr[6].u_ar_a` … `g_bg_addr[6].u_ar_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_addr[6].u_ar.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_addr[6].u_ar.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_addr[6].u_ar.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 4. `q[14]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_addr[5].u_ar(MPS_reg_rst_we$_WIDTH15) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_addr[5].u_ar` into 2 copies (`g_bg_addr[5].u_ar_a` … `g_bg_addr[5].u_ar_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_addr[5].u_ar.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_addr[5].u_ar.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_addr[5].u_ar.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 5. `q[14]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_addr[4].u_ar(MPS_reg_rst_we$_WIDTH15) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_addr[4].u_ar` into 2 copies (`g_bg_addr[4].u_ar_a` … `g_bg_addr[4].u_ar_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_addr[4].u_ar.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_addr[4].u_ar.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_addr[4].u_ar.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 6. `q[14]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_addr[3].u_ar(MPS_reg_rst_we$_WIDTH15) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_addr[3].u_ar` into 2 copies (`g_bg_addr[3].u_ar_a` … `g_bg_addr[3].u_ar_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_addr[3].u_ar.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_addr[3].u_ar.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_addr[3].u_ar.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 7. `q[14]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_addr[2].u_ar(MPS_reg_rst_we$_WIDTH15) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_addr[2].u_ar` into 2 copies (`g_bg_addr[2].u_ar_a` … `g_bg_addr[2].u_ar_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_addr[2].u_ar.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_addr[2].u_ar.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_addr[2].u_ar.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 8. `q[14]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_addr[1].u_ar(MPS_reg_rst_we$_WIDTH15) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_addr[1].u_ar` into 2 copies (`g_bg_addr[1].u_ar_a` … `g_bg_addr[1].u_ar_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_addr[1].u_ar.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_addr[1].u_ar.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_addr[1].u_ar.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 9. `q[14]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_addr[0].u_ar(MPS_reg_rst_we$_WIDTH15) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_addr[0].u_ar` into 2 copies (`g_bg_addr[0].u_ar_a` … `g_bg_addr[0].u_ar_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_addr[0].u_ar.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_addr[0].u_ar.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_addr[0].u_ar.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 10. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[63].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 11. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[62].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 12. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[61].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 13. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[60].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 14. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[59].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 15. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[58].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 16. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[57].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 17. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[56].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 18. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[55].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 19. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[54].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 20. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[53].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 21. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[52].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 22. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[51].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 23. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[50].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 24. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[49].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 25. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[48].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 26. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[47].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 27. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[46].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 28. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[45].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 29. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[44].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 30. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[43].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 31. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[42].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 32. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[41].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 33. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[40].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 34. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[39].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 35. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[38].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 36. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[37].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 37. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[36].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 38. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[35].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 39. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[34].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 40. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[33].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 41. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[32].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 42. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[31].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 43. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[30].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 44. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[29].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 45. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[28].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 46. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[27].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 47. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[26].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 48. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[25].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 49. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[24].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 50. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[23].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 51. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[22].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 52. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[21].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 53. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[20].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 54. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[19].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 55. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[18].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 56. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[17].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 57. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[16].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 58. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[15].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 59. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[14].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 60. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[13].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 61. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[12].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 62. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[11].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 63. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[10].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 64. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[9].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 65. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[8].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 66. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[7].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 67. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[6].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 68. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[5].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 69. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[4].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 70. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[3].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 71. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[2].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 72. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[1].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 73. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_ss(and2_N$_WIDTH64) → g_and_N[0].u0(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_ss` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_ss` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `u_ss` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_ss.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_ss.out` in `<file for mem_controller>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `u_ss.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 74. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b15(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b15` into 2 copies (`g_bg_wb[7].u_b15_a` … `g_bg_wb[7].u_b15_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b15.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b15.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b15.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 75. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b14(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b14` into 2 copies (`g_bg_wb[7].u_b14_a` … `g_bg_wb[7].u_b14_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b14.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b14.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b14.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 76. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b13(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b13` into 2 copies (`g_bg_wb[7].u_b13_a` … `g_bg_wb[7].u_b13_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b13.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b13.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b13.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 77. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b12(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b12` into 2 copies (`g_bg_wb[7].u_b12_a` … `g_bg_wb[7].u_b12_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b12.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b12.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b12.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 78. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b11(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b11` into 2 copies (`g_bg_wb[7].u_b11_a` … `g_bg_wb[7].u_b11_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b11.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b11.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b11.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 79. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b10(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b10` into 2 copies (`g_bg_wb[7].u_b10_a` … `g_bg_wb[7].u_b10_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b10.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b10.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b10.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 80. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b9(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b9` into 2 copies (`g_bg_wb[7].u_b9_a` … `g_bg_wb[7].u_b9_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b9.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b9.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b9.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 81. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b8(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b8` into 2 copies (`g_bg_wb[7].u_b8_a` … `g_bg_wb[7].u_b8_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b8.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b8.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b8.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 82. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b7(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b7` into 2 copies (`g_bg_wb[7].u_b7_a` … `g_bg_wb[7].u_b7_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b7.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b7.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b7.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 83. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b6(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b6` into 2 copies (`g_bg_wb[7].u_b6_a` … `g_bg_wb[7].u_b6_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b6.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b6.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b6.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 84. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b5(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b5` into 2 copies (`g_bg_wb[7].u_b5_a` … `g_bg_wb[7].u_b5_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b5.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b5.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b5.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 85. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b4(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b4` into 2 copies (`g_bg_wb[7].u_b4_a` … `g_bg_wb[7].u_b4_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b4.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b4.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b4.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 86. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b3(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b3` into 2 copies (`g_bg_wb[7].u_b3_a` … `g_bg_wb[7].u_b3_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b3.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b3.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b3.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 87. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b2(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b2` into 2 copies (`g_bg_wb[7].u_b2_a` … `g_bg_wb[7].u_b2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b2.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 88. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b1(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b1` into 2 copies (`g_bg_wb[7].u_b1_a` … `g_bg_wb[7].u_b1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b1.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 89. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[7].u_b0(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[7].u_b0` into 2 copies (`g_bg_wb[7].u_b0_a` … `g_bg_wb[7].u_b0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[7].u_b0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[7].u_b0.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[7].u_b0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 90. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b15(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b15` into 2 copies (`g_bg_wb[6].u_b15_a` … `g_bg_wb[6].u_b15_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b15.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b15.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b15.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 91. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b14(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b14` into 2 copies (`g_bg_wb[6].u_b14_a` … `g_bg_wb[6].u_b14_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b14.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b14.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b14.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 92. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b13(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b13` into 2 copies (`g_bg_wb[6].u_b13_a` … `g_bg_wb[6].u_b13_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b13.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b13.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b13.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 93. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b12(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b12` into 2 copies (`g_bg_wb[6].u_b12_a` … `g_bg_wb[6].u_b12_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b12.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b12.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b12.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 94. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b11(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b11` into 2 copies (`g_bg_wb[6].u_b11_a` … `g_bg_wb[6].u_b11_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b11.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b11.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b11.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 95. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b10(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b10` into 2 copies (`g_bg_wb[6].u_b10_a` … `g_bg_wb[6].u_b10_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b10.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b10.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b10.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 96. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b9(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b9` into 2 copies (`g_bg_wb[6].u_b9_a` … `g_bg_wb[6].u_b9_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b9.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b9.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b9.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 97. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b8(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b8` into 2 copies (`g_bg_wb[6].u_b8_a` … `g_bg_wb[6].u_b8_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b8.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b8.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b8.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 98. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b7(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b7` into 2 copies (`g_bg_wb[6].u_b7_a` … `g_bg_wb[6].u_b7_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b7.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b7.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b7.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 99. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b6(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b6` into 2 copies (`g_bg_wb[6].u_b6_a` … `g_bg_wb[6].u_b6_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b6.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b6.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b6.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 100. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b5(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b5` into 2 copies (`g_bg_wb[6].u_b5_a` … `g_bg_wb[6].u_b5_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b5.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b5.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b5.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 101. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b4(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b4` into 2 copies (`g_bg_wb[6].u_b4_a` … `g_bg_wb[6].u_b4_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b4.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b4.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b4.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 102. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b3(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b3` into 2 copies (`g_bg_wb[6].u_b3_a` … `g_bg_wb[6].u_b3_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b3.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b3.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b3.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 103. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b2(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b2` into 2 copies (`g_bg_wb[6].u_b2_a` … `g_bg_wb[6].u_b2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b2.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 104. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b1(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b1` into 2 copies (`g_bg_wb[6].u_b1_a` … `g_bg_wb[6].u_b1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b1.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 105. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[6].u_b0(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[6].u_b0` into 2 copies (`g_bg_wb[6].u_b0_a` … `g_bg_wb[6].u_b0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[6].u_b0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[6].u_b0.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[6].u_b0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 106. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b15(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b15` into 2 copies (`g_bg_wb[5].u_b15_a` … `g_bg_wb[5].u_b15_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b15.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b15.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b15.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 107. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b14(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b14` into 2 copies (`g_bg_wb[5].u_b14_a` … `g_bg_wb[5].u_b14_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b14.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b14.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b14.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 108. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b13(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b13` into 2 copies (`g_bg_wb[5].u_b13_a` … `g_bg_wb[5].u_b13_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b13.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b13.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b13.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 109. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b12(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b12` into 2 copies (`g_bg_wb[5].u_b12_a` … `g_bg_wb[5].u_b12_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b12.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b12.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b12.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 110. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b11(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b11` into 2 copies (`g_bg_wb[5].u_b11_a` … `g_bg_wb[5].u_b11_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b11.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b11.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b11.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 111. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b10(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b10` into 2 copies (`g_bg_wb[5].u_b10_a` … `g_bg_wb[5].u_b10_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b10.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b10.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b10.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 112. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b9(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b9` into 2 copies (`g_bg_wb[5].u_b9_a` … `g_bg_wb[5].u_b9_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b9.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b9.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b9.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 113. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b8(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b8` into 2 copies (`g_bg_wb[5].u_b8_a` … `g_bg_wb[5].u_b8_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b8.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b8.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b8.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 114. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b7(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b7` into 2 copies (`g_bg_wb[5].u_b7_a` … `g_bg_wb[5].u_b7_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b7.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b7.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b7.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 115. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b6(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b6` into 2 copies (`g_bg_wb[5].u_b6_a` … `g_bg_wb[5].u_b6_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b6.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b6.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b6.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 116. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b5(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b5` into 2 copies (`g_bg_wb[5].u_b5_a` … `g_bg_wb[5].u_b5_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b5.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b5.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b5.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 117. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b4(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b4` into 2 copies (`g_bg_wb[5].u_b4_a` … `g_bg_wb[5].u_b4_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b4.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b4.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b4.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 118. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b3(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b3` into 2 copies (`g_bg_wb[5].u_b3_a` … `g_bg_wb[5].u_b3_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b3.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b3.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b3.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 119. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b2(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b2` into 2 copies (`g_bg_wb[5].u_b2_a` … `g_bg_wb[5].u_b2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b2.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 120. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b1(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b1` into 2 copies (`g_bg_wb[5].u_b1_a` … `g_bg_wb[5].u_b1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b1.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 121. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[5].u_b0(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[5].u_b0` into 2 copies (`g_bg_wb[5].u_b0_a` … `g_bg_wb[5].u_b0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[5].u_b0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[5].u_b0.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[5].u_b0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 122. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b15(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b15` into 2 copies (`g_bg_wb[4].u_b15_a` … `g_bg_wb[4].u_b15_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b15.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b15.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b15.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 123. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b14(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b14` into 2 copies (`g_bg_wb[4].u_b14_a` … `g_bg_wb[4].u_b14_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b14.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b14.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b14.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 124. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b13(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b13` into 2 copies (`g_bg_wb[4].u_b13_a` … `g_bg_wb[4].u_b13_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b13.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b13.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b13.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 125. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b12(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b12` into 2 copies (`g_bg_wb[4].u_b12_a` … `g_bg_wb[4].u_b12_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b12.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b12.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b12.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 126. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b11(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b11` into 2 copies (`g_bg_wb[4].u_b11_a` … `g_bg_wb[4].u_b11_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b11.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b11.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b11.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 127. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b10(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b10` into 2 copies (`g_bg_wb[4].u_b10_a` … `g_bg_wb[4].u_b10_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b10.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b10.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b10.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 128. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b9(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b9` into 2 copies (`g_bg_wb[4].u_b9_a` … `g_bg_wb[4].u_b9_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b9.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b9.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b9.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 129. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b8(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b8` into 2 copies (`g_bg_wb[4].u_b8_a` … `g_bg_wb[4].u_b8_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b8.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b8.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b8.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 130. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b7(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b7` into 2 copies (`g_bg_wb[4].u_b7_a` … `g_bg_wb[4].u_b7_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b7.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b7.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b7.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 131. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b6(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b6` into 2 copies (`g_bg_wb[4].u_b6_a` … `g_bg_wb[4].u_b6_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b6.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b6.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b6.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 132. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b5(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b5` into 2 copies (`g_bg_wb[4].u_b5_a` … `g_bg_wb[4].u_b5_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b5.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b5.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b5.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 133. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b4(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b4` into 2 copies (`g_bg_wb[4].u_b4_a` … `g_bg_wb[4].u_b4_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b4.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b4.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b4.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 134. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b3(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b3` into 2 copies (`g_bg_wb[4].u_b3_a` … `g_bg_wb[4].u_b3_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b3.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b3.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b3.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 135. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b2(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b2` into 2 copies (`g_bg_wb[4].u_b2_a` … `g_bg_wb[4].u_b2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b2.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 136. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b1(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b1` into 2 copies (`g_bg_wb[4].u_b1_a` … `g_bg_wb[4].u_b1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b1.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 137. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[4].u_b0(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[4].u_b0` into 2 copies (`g_bg_wb[4].u_b0_a` … `g_bg_wb[4].u_b0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[4].u_b0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[4].u_b0.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[4].u_b0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 138. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b15(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b15` into 2 copies (`g_bg_wb[3].u_b15_a` … `g_bg_wb[3].u_b15_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b15.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b15.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b15.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 139. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b14(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b14` into 2 copies (`g_bg_wb[3].u_b14_a` … `g_bg_wb[3].u_b14_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b14.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b14.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b14.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 140. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b13(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b13` into 2 copies (`g_bg_wb[3].u_b13_a` … `g_bg_wb[3].u_b13_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b13.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b13.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b13.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 141. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b12(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b12` into 2 copies (`g_bg_wb[3].u_b12_a` … `g_bg_wb[3].u_b12_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b12.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b12.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b12.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 142. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b11(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b11` into 2 copies (`g_bg_wb[3].u_b11_a` … `g_bg_wb[3].u_b11_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b11.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b11.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b11.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 143. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b10(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b10` into 2 copies (`g_bg_wb[3].u_b10_a` … `g_bg_wb[3].u_b10_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b10.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b10.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b10.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 144. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b9(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b9` into 2 copies (`g_bg_wb[3].u_b9_a` … `g_bg_wb[3].u_b9_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b9.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b9.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b9.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 145. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b8(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b8` into 2 copies (`g_bg_wb[3].u_b8_a` … `g_bg_wb[3].u_b8_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b8.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b8.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b8.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 146. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b7(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b7` into 2 copies (`g_bg_wb[3].u_b7_a` … `g_bg_wb[3].u_b7_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b7.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b7.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b7.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 147. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b6(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b6` into 2 copies (`g_bg_wb[3].u_b6_a` … `g_bg_wb[3].u_b6_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b6.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b6.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b6.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 148. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b5(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b5` into 2 copies (`g_bg_wb[3].u_b5_a` … `g_bg_wb[3].u_b5_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b5.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b5.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b5.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 149. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b4(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b4` into 2 copies (`g_bg_wb[3].u_b4_a` … `g_bg_wb[3].u_b4_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b4.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b4.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b4.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 150. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b3(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b3` into 2 copies (`g_bg_wb[3].u_b3_a` … `g_bg_wb[3].u_b3_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b3.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b3.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b3.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 151. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b2(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b2` into 2 copies (`g_bg_wb[3].u_b2_a` … `g_bg_wb[3].u_b2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b2.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 152. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b1(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b1` into 2 copies (`g_bg_wb[3].u_b1_a` … `g_bg_wb[3].u_b1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b1.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 153. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[3].u_b0(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[3].u_b0` into 2 copies (`g_bg_wb[3].u_b0_a` … `g_bg_wb[3].u_b0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[3].u_b0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[3].u_b0.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[3].u_b0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 154. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b15(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b15` into 2 copies (`g_bg_wb[2].u_b15_a` … `g_bg_wb[2].u_b15_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b15.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b15.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b15.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 155. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b14(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b14` into 2 copies (`g_bg_wb[2].u_b14_a` … `g_bg_wb[2].u_b14_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b14.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b14.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b14.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 156. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b13(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b13` into 2 copies (`g_bg_wb[2].u_b13_a` … `g_bg_wb[2].u_b13_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b13.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b13.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b13.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 157. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b12(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b12` into 2 copies (`g_bg_wb[2].u_b12_a` … `g_bg_wb[2].u_b12_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b12.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b12.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b12.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 158. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b11(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b11` into 2 copies (`g_bg_wb[2].u_b11_a` … `g_bg_wb[2].u_b11_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b11.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b11.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b11.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 159. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b10(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b10` into 2 copies (`g_bg_wb[2].u_b10_a` … `g_bg_wb[2].u_b10_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b10.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b10.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b10.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 160. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b9(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b9` into 2 copies (`g_bg_wb[2].u_b9_a` … `g_bg_wb[2].u_b9_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b9.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b9.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b9.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 161. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b8(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b8` into 2 copies (`g_bg_wb[2].u_b8_a` … `g_bg_wb[2].u_b8_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b8.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b8.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b8.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 162. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b7(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b7` into 2 copies (`g_bg_wb[2].u_b7_a` … `g_bg_wb[2].u_b7_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b7.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b7.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b7.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 163. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b6(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b6` into 2 copies (`g_bg_wb[2].u_b6_a` … `g_bg_wb[2].u_b6_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b6.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b6.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b6.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 164. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b5(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b5` into 2 copies (`g_bg_wb[2].u_b5_a` … `g_bg_wb[2].u_b5_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b5.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b5.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b5.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 165. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b4(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b4` into 2 copies (`g_bg_wb[2].u_b4_a` … `g_bg_wb[2].u_b4_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b4.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b4.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b4.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 166. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b3(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b3` into 2 copies (`g_bg_wb[2].u_b3_a` … `g_bg_wb[2].u_b3_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b3.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b3.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b3.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 167. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b2(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b2` into 2 copies (`g_bg_wb[2].u_b2_a` … `g_bg_wb[2].u_b2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b2.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 168. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b1(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b1` into 2 copies (`g_bg_wb[2].u_b1_a` … `g_bg_wb[2].u_b1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b1.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 169. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[2].u_b0(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[2].u_b0` into 2 copies (`g_bg_wb[2].u_b0_a` … `g_bg_wb[2].u_b0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[2].u_b0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[2].u_b0.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[2].u_b0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 170. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b15(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b15` into 2 copies (`g_bg_wb[1].u_b15_a` … `g_bg_wb[1].u_b15_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b15.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b15.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b15.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 171. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b14(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b14` into 2 copies (`g_bg_wb[1].u_b14_a` … `g_bg_wb[1].u_b14_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b14.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b14.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b14.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 172. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b13(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b13` into 2 copies (`g_bg_wb[1].u_b13_a` … `g_bg_wb[1].u_b13_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b13.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b13.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b13.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 173. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b12(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b12` into 2 copies (`g_bg_wb[1].u_b12_a` … `g_bg_wb[1].u_b12_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b12.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b12.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b12.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 174. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b11(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b11` into 2 copies (`g_bg_wb[1].u_b11_a` … `g_bg_wb[1].u_b11_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b11.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b11.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b11.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 175. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b10(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b10` into 2 copies (`g_bg_wb[1].u_b10_a` … `g_bg_wb[1].u_b10_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b10.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b10.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b10.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 176. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b9(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b9` into 2 copies (`g_bg_wb[1].u_b9_a` … `g_bg_wb[1].u_b9_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b9.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b9.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b9.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 177. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b8(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b8` into 2 copies (`g_bg_wb[1].u_b8_a` … `g_bg_wb[1].u_b8_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b8.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b8.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b8.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 178. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b7(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b7` into 2 copies (`g_bg_wb[1].u_b7_a` … `g_bg_wb[1].u_b7_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b7.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b7.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b7.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 179. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b6(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b6` into 2 copies (`g_bg_wb[1].u_b6_a` … `g_bg_wb[1].u_b6_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b6.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b6.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b6.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 180. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b5(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b5` into 2 copies (`g_bg_wb[1].u_b5_a` … `g_bg_wb[1].u_b5_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b5.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b5.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b5.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 181. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b4(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b4` into 2 copies (`g_bg_wb[1].u_b4_a` … `g_bg_wb[1].u_b4_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b4.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b4.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b4.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 182. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b3(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b3` into 2 copies (`g_bg_wb[1].u_b3_a` … `g_bg_wb[1].u_b3_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b3.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b3.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b3.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 183. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b2(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b2` into 2 copies (`g_bg_wb[1].u_b2_a` … `g_bg_wb[1].u_b2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b2.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 184. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b1(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b1` into 2 copies (`g_bg_wb[1].u_b1_a` … `g_bg_wb[1].u_b1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b1.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 185. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[1].u_b0(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[1].u_b0` into 2 copies (`g_bg_wb[1].u_b0_a` … `g_bg_wb[1].u_b0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[1].u_b0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[1].u_b0.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[1].u_b0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 186. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b15(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b15` into 2 copies (`g_bg_wb[0].u_b15_a` … `g_bg_wb[0].u_b15_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b15.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b15.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b15.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 187. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b14(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b14` into 2 copies (`g_bg_wb[0].u_b14_a` … `g_bg_wb[0].u_b14_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b14.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b14.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b14.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 188. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b13(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b13` into 2 copies (`g_bg_wb[0].u_b13_a` … `g_bg_wb[0].u_b13_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b13.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b13.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b13.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 189. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b12(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b12` into 2 copies (`g_bg_wb[0].u_b12_a` … `g_bg_wb[0].u_b12_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b12.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b12.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b12.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 190. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b11(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b11` into 2 copies (`g_bg_wb[0].u_b11_a` … `g_bg_wb[0].u_b11_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b11.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b11.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b11.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 191. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b10(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b10` into 2 copies (`g_bg_wb[0].u_b10_a` … `g_bg_wb[0].u_b10_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b10.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b10.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b10.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 192. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b9(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b9` into 2 copies (`g_bg_wb[0].u_b9_a` … `g_bg_wb[0].u_b9_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b9.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b9.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b9.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 193. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b8(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b8` into 2 copies (`g_bg_wb[0].u_b8_a` … `g_bg_wb[0].u_b8_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b8.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b8.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b8.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 194. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b7(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b7` into 2 copies (`g_bg_wb[0].u_b7_a` … `g_bg_wb[0].u_b7_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b7.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b7.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b7.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 195. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b6(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b6` into 2 copies (`g_bg_wb[0].u_b6_a` … `g_bg_wb[0].u_b6_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b6.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b6.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b6.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 196. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b5(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b5` into 2 copies (`g_bg_wb[0].u_b5_a` … `g_bg_wb[0].u_b5_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b5.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b5.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b5.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 197. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b4(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b4` into 2 copies (`g_bg_wb[0].u_b4_a` … `g_bg_wb[0].u_b4_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b4.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b4.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b4.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 198. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b3(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b3` into 2 copies (`g_bg_wb[0].u_b3_a` … `g_bg_wb[0].u_b3_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b3.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b3.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b3.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 199. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b2(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b2` into 2 copies (`g_bg_wb[0].u_b2_a` … `g_bg_wb[0].u_b2_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b2.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b2.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b2.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 200. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b1(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b1` into 2 copies (`g_bg_wb[0].u_b1_a` … `g_bg_wb[0].u_b1_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b1.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b1.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b1.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 201. `q[7]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → g_bg_wb[0].u_b0(MPS_reg_rst_we$_WIDTH8) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Register duplication × 2 | 0.00 ns | In `<file for mem_controller>` clone `g_bg_wb[0].u_b0` into 2 copies (`g_bg_wb[0].u_b0_a` … `g_bg_wb[0].u_b0_b`); each copy drives ~4 loads. **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side (D)**: fanout = 2 on the next-state wire → no input buffer. clk/rst are clock-tree distributed so they don't count. Total added delay on a load path = 0.00 + 0.00 = **0.00 ns**. This assumes D was fanout-1 before duplication; if the next-state expression already feeds other loads, the input-side cost may be smaller (existing buffer reused) or larger (extra cascade). |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (single inverter, **0.15 ns**) only if every downstream consumer of `g_bg_wb[0].u_b0.q` can absorb the inversion (NAND↔AND swap, mux input swap, etc.). Otherwise pair-cost = 0.30 ns and `bufferH16$` is cheaper. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` between `g_bg_wb[0].u_b0.q` and its loads in `<file for mem_controller>`. The buffer's input is a single load on `g_bg_wb[0].u_b0.q` (fanout-1, no upstream cascade). Adds **0.24 ns** on every path through this signal. |

### 202. `out` (fanout=5) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_hit(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_hit` (a `and3$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_hit` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_hit` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_hit.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_hit.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_hit.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 203. `out` (fanout=80) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u0_fsm(mem_controller_fsm) → ld_address_changed_o_and(and7_N$_WIDTH1) → g[0].u(MPS_AND_IN7$) → g3(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.30 ns | In `<file for mem_controller_fsm>` instantiate `ld_address_changed_o_and` (a `and3$`-class gate) 2 times with the same fanin; split loads ~40 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `ld_address_changed_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 40 → `bufferH64$` (0.30 ns). **Input side**: each fan-in wire of `ld_address_changed_o_and` now has +1 extra loads → no input buffer. Total added delay = 0.30 + 0.00 = **0.30 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller_fsm>`. |
| alt 1 | Buffer with bufferHInv256$ (polarity flip needed) | 0.45 ns | Use `bufferHInv256$` (inverting, **0.45 ns**) only if every consumer of `ld_address_changed_o_and.out` can absorb the inversion. Pair cost = 0.90 ns. |
| alt 2 | Buffer with bufferH256$ | 0.54 ns | Insert `bufferH256$` after `ld_address_changed_o_and.out` in `<file for mem_controller_fsm>`; routes all 80 loads through one buffer. The buffer's input is a fanout-1 load on `ld_address_changed_o_and.out` (no upstream cascade). Adds **0.54 ns** to every load path. |

### 204. `out` (fanout=16) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u0_fsm(mem_controller_fsm) → set_WriteBuf_V_o_and(and6_N$_WIDTH1) → g[0].u(MPS_AND_IN6$) → g2(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 4 (or duplicate source register × 4) | 0.00 ns | In `<file for mem_controller_fsm>` instantiate `set_WriteBuf_V_o_and` (a `mux2$`-class gate) 4 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `set_WriteBuf_V_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `set_WriteBuf_V_o_and` now has +3 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller_fsm>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `set_WriteBuf_V_o_and.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `set_WriteBuf_V_o_and.out` in `<file for mem_controller_fsm>`; routes all 16 loads through one buffer. The buffer's input is a fanout-1 load on `set_WriteBuf_V_o_and.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 205. `out` (fanout=8) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u0_fsm(mem_controller_fsm) → fill0_o_and(and6_N$_WIDTH1) → g[0].u(MPS_AND_IN6$) → g2(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller_fsm>` instantiate `fill0_o_and` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `fill0_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `fill0_o_and` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller_fsm>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `fill0_o_and.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `fill0_o_and.out` in `<file for mem_controller_fsm>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `fill0_o_and.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 206. `out` (fanout=8) — driven by `and4$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u0_fsm(mem_controller_fsm) → fill1_o_and(and4_N$_WIDTH1) → g[0].u(MPS_AND_IN4) → g0(and4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller_fsm>` instantiate `fill1_o_and` (a `and4$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `fill1_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `fill1_o_and` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller_fsm>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `fill1_o_and.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `fill1_o_and.out` in `<file for mem_controller_fsm>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `fill1_o_and.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 207. `out` (fanout=8) — driven by `and4$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u0_fsm(mem_controller_fsm) → fill2_o_and(and4_N$_WIDTH1) → g[0].u(MPS_AND_IN4) → g0(and4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller_fsm>` instantiate `fill2_o_and` (a `and4$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `fill2_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `fill2_o_and` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller_fsm>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `fill2_o_and.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `fill2_o_and.out` in `<file for mem_controller_fsm>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `fill2_o_and.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 208. `out` (fanout=8) — driven by `and4$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u0_fsm(mem_controller_fsm) → fill3_o_and(and4_N$_WIDTH1) → g[0].u(MPS_AND_IN4) → g0(and4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller_fsm>` instantiate `fill3_o_and` (a `and4$`-class gate) 2 times with the same fanin; split loads ~4 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `fill3_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 4 → no per-copy buffer. **Input side**: each fan-in wire of `fill3_o_and` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller_fsm>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `fill3_o_and.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `fill3_o_and.out` in `<file for mem_controller_fsm>`; routes all 8 loads through one buffer. The buffer's input is a fanout-1 load on `fill3_o_and.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 209. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[3].GEN_LOW[3].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 210. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[3].GEN_LOW[2].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 211. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[3].GEN_LOW[1].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 212. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[3].GEN_LOW[0].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 213. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[2].GEN_LOW[3].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 214. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[2].GEN_LOW[2].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 215. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[2].GEN_LOW[1].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 216. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[2].GEN_LOW[0].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 217. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[1].GEN_LOW[3].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 218. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[1].GEN_LOW[2].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 219. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[1].GEN_LOW[1].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 220. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[1].GEN_LOW[0].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 221. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[0].GEN_LOW[3].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 222. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[0].GEN_LOW[2].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 223. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[0].GEN_LOW[1].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 224. `out` (fanout=5) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_chip_dec(MPS_decoder$_INPUTS4) → GEN_DEC_N.GEN_HIGH[0].GEN_LOW[0].u_and(MPS_AND_IN2) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for mem_controller>` instantiate `u_chip_dec` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_chip_dec` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_chip_dec` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_chip_dec.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_chip_dec.out` in `<file for mem_controller>`; routes all 5 loads through one buffer. The buffer's input is a fanout-1 load on `u_chip_dec.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 225. `out[7]` (fanout=6) — driven by `decoder3_8$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_bg_dec(MPS_decoder$_INPUTS3) → GEN_DEC3.u_dec(decoder3_8$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Insert bufferH16$ | 0.24 ns | Driver class for `decoder3_8$` not specifically modeled. Default: insert `bufferH16$` (0.24 ns). |

### 226. `out[0]` (fanout=64) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u0_fsm(mem_controller_fsm) → set_ld_tristate_o_or(or5_N$_WIDTH1) → g_or5[0].u0(MPS_OR_IN5$) → g3(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 4 (or duplicate source register × 4) | 0.24 ns | In `<file for mem_controller_fsm>` instantiate `set_ld_tristate_o_or` (a `nand3$`-class gate) 4 times with the same fanin; split loads ~16 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `set_ld_tristate_o_or` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 16 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `set_ld_tristate_o_or` now has +3 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller_fsm>`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `set_ld_tristate_o_or.out` in `<file for mem_controller_fsm>`; routes all 64 loads through one buffer. The buffer's input is a fanout-1 load on `set_ld_tristate_o_or.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `set_ld_tristate_o_or.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 227. `out` (fanout=64) — driven by `and4$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u0_fsm(mem_controller_fsm) → start_store_o_and(and4_N$_WIDTH1) → g[0].u(MPS_AND_IN4) → g0(and4$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 4 (or duplicate source register × 4) | 0.24 ns | In `<file for mem_controller_fsm>` instantiate `start_store_o_and` (a `and4$`-class gate) 4 times with the same fanin; split loads ~16 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `start_store_o_and` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 16 → `bufferH16$` (0.24 ns). **Input side**: each fan-in wire of `start_store_o_and` now has +3 extra loads → no input buffer. Total added delay = 0.24 + 0.00 = **0.24 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for mem_controller_fsm>`. |
| alt 1 | Buffer with bufferH64$ | 0.30 ns | Insert `bufferH64$` after `start_store_o_and.out` in `<file for mem_controller_fsm>`; routes all 64 loads through one buffer. The buffer's input is a fanout-1 load on `start_store_o_and.out` (no upstream cascade). Adds **0.30 ns** to every load path. |
| alt 2 | Buffer with bufferHInv64$ (polarity flip needed) | 0.39 ns | Use `bufferHInv64$` (inverting, **0.39 ns**) only if every consumer of `start_store_o_and.out` can absorb the inversion. Pair cost = 0.78 ns. |

### 228. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[0].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 229. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[0].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 230. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[0].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 231. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[1].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 232. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[1].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 233. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[1].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 234. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[2].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 235. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[2].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 236. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[2].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 237. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[3].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 238. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[3].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 239. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[3].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 240. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[4].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 241. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[4].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 242. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[4].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 243. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[5].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 244. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[5].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 245. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[5].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 246. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[6].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 247. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[6].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 248. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[6].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 249. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[7].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 250. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[7].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 251. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[7].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 252. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[8].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 253. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[8].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 254. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[8].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 255. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[9].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 256. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[9].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 257. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[9].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 258. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[10].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 259. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[10].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 260. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[10].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 261. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[11].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 262. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[11].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 263. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[11].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 264. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[12].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 265. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[12].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 266. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[12].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 267. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[13].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 268. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[13].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 269. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[13].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 270. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[14].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 271. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[14].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 272. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[14].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 273. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[15].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 274. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[15].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 275. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[15].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 276. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[16].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 277. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[16].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 278. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[16].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 279. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[17].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 280. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[17].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 281. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[17].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 282. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[18].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 283. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[18].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 284. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[18].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 285. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[19].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 286. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[19].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 287. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[19].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 288. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[20].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 289. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[20].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 290. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[20].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 291. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[21].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 292. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[21].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 293. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[21].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 294. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[22].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 295. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[22].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 296. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[22].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 297. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[23].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 298. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[23].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 299. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[23].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 300. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[24].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 301. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[24].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 302. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[24].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 303. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[25].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 304. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[25].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 305. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[25].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 306. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[26].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 307. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[26].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 308. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[26].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 309. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[27].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 310. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[27].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 311. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[27].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 312. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[28].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 313. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[28].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 314. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[28].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 315. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[29].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 316. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[29].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 317. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[29].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 318. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[30].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 319. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[30].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 320. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[30].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 321. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[31].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 322. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[31].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 323. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[31].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 324. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[32].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 325. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[32].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 326. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[32].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 327. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[33].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 328. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[33].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 329. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[33].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 330. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[34].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 331. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[34].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 332. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[34].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 333. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[35].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 334. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[35].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 335. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[35].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 336. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[36].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 337. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[36].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 338. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[36].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 339. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[37].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 340. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[37].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 341. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[37].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 342. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[38].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 343. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[38].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 344. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[38].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 345. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[39].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 346. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[39].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 347. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[39].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 348. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[40].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 349. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[40].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 350. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[40].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 351. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[41].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 352. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[41].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 353. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[41].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 354. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[42].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 355. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[42].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 356. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[42].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 357. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[43].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 358. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[43].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 359. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[43].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 360. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[44].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 361. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[44].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 362. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[44].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 363. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[45].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 364. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[45].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 365. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[45].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 366. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[46].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 367. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[46].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 368. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[46].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 369. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[47].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 370. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[47].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 371. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[47].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 372. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[48].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 373. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[48].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 374. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[48].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 375. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[49].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 376. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[49].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 377. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[49].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 378. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[50].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 379. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[50].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 380. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[50].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 381. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[51].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 382. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[51].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 383. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[51].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 384. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[52].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 385. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[52].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 386. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[52].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 387. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[53].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 388. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[53].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 389. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[53].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 390. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[54].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 391. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[54].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 392. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[54].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 393. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[55].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 394. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[55].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 395. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[55].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 396. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[56].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 397. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[56].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 398. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[56].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 399. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[57].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 400. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[57].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 401. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[57].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 402. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[58].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 403. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[58].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 404. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[58].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 405. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[59].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 406. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[59].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 407. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[59].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 408. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[60].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 409. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[60].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 410. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[60].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 411. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[61].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 412. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[61].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 413. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[61].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 414. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[62].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 415. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[62].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 416. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[62].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 417. `WE_o` (fanout=132) — driven by `bufferH64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[63].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_WE_o_buf(bufferH64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 418. `out[0]` (fanout=128) — driven by `bufferHInv64$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[63].mem_bank(mem_bank) → u3(inv_N$_WIDTH1) → GEN_INV[0].u_inv(bufferHInv64$)
- **Required tier**: `bufferH256$ / bufferHInv256$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 419. `S_3` (fanout=18) — driven by `bufferH16$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → g_mem_banks[63].mem_bank(mem_bank) → u0_Controller(bank_controller_fsm_logic) → u_S_3_buf(bufferH16$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Already buffered | 0.00 ns | Driver is itself a buffer. Likely an over-buffering INFO -- either downsize the buffer or accept the slack. |

### 420. `out[7]` (fanout=8) — driven by `decoder3_8$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_bank_dec(MPS_decoder$_INPUTS6) → GEN_DEC_N.u_high(MPS_decoder$_INPUTS3) → GEN_DEC3.u_dec(decoder3_8$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Insert bufferH16$ | 0.24 ns | Driver class for `decoder3_8$` not specifically modeled. Default: insert `bufferH16$` (0.24 ns). |

### 421. `out[7]` (fanout=8) — driven by `decoder3_8$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_bank_dec(MPS_decoder$_INPUTS6) → GEN_DEC_N.u_low(MPS_decoder$_INPUTS3) → GEN_DEC3.u_dec(decoder3_8$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Insert bufferH16$ | 0.24 ns | Driver class for `decoder3_8$` not specifically modeled. Default: insert `bufferH16$` (0.24 ns). |

### 422. `out[7]` (fanout=8) — driven by `decoder3_8$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_store_dec(MPS_decoder$_INPUTS6) → GEN_DEC_N.u_high(MPS_decoder$_INPUTS3) → GEN_DEC3.u_dec(decoder3_8$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Insert bufferH16$ | 0.24 ns | Driver class for `decoder3_8$` not specifically modeled. Default: insert `bufferH16$` (0.24 ns). |

### 423. `out[7]` (fanout=8) — driven by `decoder3_8$`

- **Driver path**: (Everywhere_TOP) → mem_unit(mem_TOP) → u_controller(mem_controller) → u_store_dec(MPS_decoder$_INPUTS6) → GEN_DEC_N.u_low(MPS_decoder$_INPUTS3) → GEN_DEC3.u_dec(decoder3_8$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Insert bufferH16$ | 0.24 ns | Driver class for `decoder3_8$` not specifically modeled. Default: insert `bufferH16$` (0.24 ns). |

---

## Cross-module violations affecting `mem` (43)

Driver lives outside the requested module but the driver-side module name suggests loads in `mem`. Recommended fix is normally on the driver side; alternatively, re-buffer at the `mem` input port.

### 1. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_0(DTE_MEM_2_DCache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 2. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_1(DTE_MEM_2_DCache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 3. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_2(DTE_MEM_2_DCache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 4. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_3(DTE_MEM_2_DCache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 5. `out[0]` (fanout=16) — driven by `nand4$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_0(DTE_MEM_2_DCache_FSM) → Drive_Addr_Bus_o_nand(nand4_N$_WIDTH1) → g[0].u(nand4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 6. `out[0]` (fanout=16) — driven by `nand4$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_1(DTE_MEM_2_DCache_FSM) → Drive_Addr_Bus_o_nand(nand4_N$_WIDTH1) → g[0].u(nand4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 7. `out[0]` (fanout=16) — driven by `nand4$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_2(DTE_MEM_2_DCache_FSM) → Drive_Addr_Bus_o_nand(nand4_N$_WIDTH1) → g[0].u(nand4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 8. `out[0]` (fanout=16) — driven by `nand4$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_3(DTE_MEM_2_DCache_FSM) → Drive_Addr_Bus_o_nand(nand4_N$_WIDTH1) → g[0].u(nand4$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 9. `out[0]` (fanout=8) — driven by `nand3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → mem_valid_o_nand(nand3_N$_WIDTH1) → g[0].u0(nand3$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 10. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_3_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

### 11. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_2_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

### 12. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_1_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

### 13. `out` (fanout=33) — driven by `and3$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → Drv_DB_0_o_and(and3_N$_WIDTH1) → g[0].u(MPS_AND_IN3) → g0(and3$)
- **Required tier**: `bufferH64$ / bufferHInv64$`

### 14. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 15. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 16. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → DTE_MEM_2_ICache(DTE_MEM_2_ICache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 17. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_0(DTE_MEM_2_DCache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 18. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_0(DTE_MEM_2_DCache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 19. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_0(DTE_MEM_2_DCache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 20. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_1(DTE_MEM_2_DCache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 21. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_1(DTE_MEM_2_DCache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 22. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_1(DTE_MEM_2_DCache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 23. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_2(DTE_MEM_2_DCache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 24. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_2(DTE_MEM_2_DCache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 25. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_2(DTE_MEM_2_DCache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 26. `q[0]` (fanout=11) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_3(DTE_MEM_2_DCache_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 27. `q[0]` (fanout=10) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_3(DTE_MEM_2_DCache_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 28. `q[0]` (fanout=9) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → mem_2_dcache_fsm_3(DTE_MEM_2_DCache_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 29. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_0(DTE_DCache_2_MEM_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 30. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_0(DTE_DCache_2_MEM_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 31. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_0(DTE_DCache_2_MEM_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 32. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_1(DTE_DCache_2_MEM_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 33. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_1(DTE_DCache_2_MEM_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 34. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_1(DTE_DCache_2_MEM_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 35. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_2(DTE_DCache_2_MEM_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 36. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_2(DTE_DCache_2_MEM_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 37. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_2(DTE_DCache_2_MEM_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 38. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_3(DTE_DCache_2_MEM_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 39. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_3(DTE_DCache_2_MEM_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 40. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dcache_2_mem_fsm_3(DTE_DCache_2_MEM_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 41. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → ff_0(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 42. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → ff_1(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

### 43. `q[0]` (fanout=8) — driven by `reg64e$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → dte_unit(DTE) → dma_2_mem_fsm(DTE_DMA_2_MEM_FSM) → ff_2(MPS_reg_rst_we$_WIDTH1) → gen_reg64[0].u_reg(reg64e$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

---

## Over-buffered INFOs (193)

Buffer rule satisfied with stronger drive than required; valid but wasteful. Optional cleanup.

- `out[0]` (fanout=15) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
