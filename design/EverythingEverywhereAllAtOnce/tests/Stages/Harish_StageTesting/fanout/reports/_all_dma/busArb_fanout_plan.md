# Fanout report -- module `busArb`

_Generated by `tests/Stages/Harish_StageTesting/fanout/scripts/fanout_optimize.py`._

> **Note on `inv_N$`**: as of this report, `lib/STDCells/UniqueLib.v` still wraps `inv1$` (drive ~1) inside `inv_N$`, **not** `bufferHInv16$`. Many recommendations assume the macro will be upgraded. If the macro has already been changed in another branch, ignore this note. Otherwise, either upgrade the macro globally (clears every inverter-driven violation at once) or instantiate `bufferHInv*$` explicitly per violation.

## Summary

- Total VIOLATIONs in check.log: **284**
- In-scope (driver inside `busArb`): **3**
- Cross-module (driver outside but module name suggests `busArb` loads): **0**
- Tristate-bus mismatches: **0**
- Boundary WARNINGs touching `busArb`: **0**
- Over-buffered INFOs in scope: **36**

### In-scope driver-class breakdown

| Driver cell | Count | Default strategy |
|---|---|---|
| `mux2$` | 3 | gate replication (0 ns) or buffer (tier delay) |

---

## In-scope violations (3)

### 1. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → dcache_picking_unit(Scheduler_DCachePicking) → u_sub_01(kogge_stone_adder_WIDTH4) → gen_stage[2].gen_bit[3].g_active.g_gray.u_gray(gray_cell) → u_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for Scheduler_DCachePicking>` instantiate `u_sub_01` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_sub_01` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_sub_01` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for Scheduler_DCachePicking>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_sub_01.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_sub_01.out` in `<file for Scheduler_DCachePicking>`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_sub_01.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 2. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → dcache_picking_unit(Scheduler_DCachePicking) → u_sub_23(kogge_stone_adder_WIDTH4) → gen_stage[2].gen_bit[3].g_active.g_gray.u_gray(gray_cell) → u_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for Scheduler_DCachePicking>` instantiate `u_sub_23` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_sub_23` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_sub_23` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for Scheduler_DCachePicking>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_sub_23.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_sub_23.out` in `<file for Scheduler_DCachePicking>`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_sub_23.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

### 3. `out` (fanout=6) — driven by `mux2$`

- **Driver path**: (Everywhere_TOP) → bus_arbitration_unit(BusArbitration) → scheduler_unit(Scheduler) → dcache_picking_unit(Scheduler_DCachePicking) → u_sub_final(kogge_stone_adder_WIDTH4) → gen_stage[2].gen_bit[3].g_active.g_gray.u_gray(gray_cell) → u_or(or2_N$_WIDTH1) → g_or2[0].u0(MPS_OR_IN2$) → g0(mux2$)
- **Required tier**: `bufferH16$ / bufferHInv16$`

| # | Option | Added delay | Action |
|---|---|---|---|
| **✅ recommended** | Replicate gate × 2 (or duplicate source register × 2) | 0.00 ns | In `<file for Scheduler_DCachePicking>` instantiate `u_sub_final` (a `mux2$`-class gate) 2 times with the same fanin; split loads ~3 per copy. **Cleaner equivalent**: trace upstream to the source register(s) feeding `u_sub_final` and clone *those*; the gate is rebuilt per copy automatically. Either way: **Output side**: per-copy fanout = 3 → no per-copy buffer. **Input side**: each fan-in wire of `u_sub_final` now has +1 extra loads → no input buffer. Total added delay = 0.00 + 0.00 = **0.00 ns**. If the gate's fanin signals were already high-fanout, the cascade may be cheaper than shown (existing buffer reused) -- worth inspecting `<file for Scheduler_DCachePicking>`. |
| alt 1 | Buffer with bufferHInv16$ (polarity flip needed) | 0.15 ns | Use `bufferHInv16$` (inverting, **0.15 ns**) only if every consumer of `u_sub_final.out` can absorb the inversion. Pair cost = 0.30 ns. |
| alt 2 | Buffer with bufferH16$ | 0.24 ns | Insert `bufferH16$` after `u_sub_final.out` in `<file for Scheduler_DCachePicking>`; routes all 6 loads through one buffer. The buffer's input is a fanout-1 load on `u_sub_final.out` (no upstream cascade). Adds **0.24 ns** to every load path. |

---

## Over-buffered INFOs (36)

Buffer rule satisfied with stronger drive than required; valid but wasteful. Optional cleanup.

- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=5) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=8) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=12) driven by `bufferHInv64$`
- `out[0]` (fanout=8) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=15) driven by `bufferHInv64$`
- `out[0]` (fanout=8) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=15) driven by `bufferHInv64$`
- `out[0]` (fanout=8) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=15) driven by `bufferHInv64$`
- `out[0]` (fanout=8) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=15) driven by `bufferHInv64$`
- `out[0]` (fanout=9) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=9) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=9) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=9) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
- `out[0]` (fanout=6) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=7) driven by `bufferHInv64$`
- `out[0]` (fanout=9) driven by `bufferHInv64$`
- `out[0]` (fanout=10) driven by `bufferHInv64$`
- `out[0]` (fanout=13) driven by `bufferHInv64$`
